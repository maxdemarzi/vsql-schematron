INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 280105_usertable
#
CREATE DATABASE `db_280105_usertable`;
USE `db_280105_usertable`;
CREATE TABLE `users` (
`id` INT,
`username` VARCHAR(64),
`password` VARCHAR(64),
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `profile` (
`id` INT,
`image` VARCHAR(64)
) CHARACTER SET latin1;
CREATE TABLE `mysports` (
`id` INT,
`baseball` INT,
`hockey` INT,
`lacrosse` INT,
`ski` INT,
`snowboard` INT,
`soccer` INT
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_280105_usertable') AS relationships;
relationships
Database: db_280105_usertable

Table Connections:
------------------
(No relationships found)

DROP DATABASE `db_280105_usertable`;
UNINSTALL EXTENSION vsql_schematron;
