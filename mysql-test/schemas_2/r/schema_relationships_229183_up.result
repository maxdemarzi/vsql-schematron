INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 229183_up
#
CREATE DATABASE `db_229183_up`;
USE `db_229183_up`;
CREATE TABLE `users` (
`user_id` VARCHAR(50) NOT NULL,
`username` TEXT,
`password_hash` TEXT,
`registration_date` TIMESTAMP NULL,
PRIMARY KEY (`user_id`)
) CHARACTER SET latin1;
CREATE TABLE `capabilities` (
`label` TEXT,
`user_id` VARCHAR(50)
) CHARACTER SET latin1;
CREATE TABLE `sessions` (
`session_id` VARCHAR(50) NOT NULL,
`user_id` VARCHAR(50),
`expiration_date` TIMESTAMP NULL,
PRIMARY KEY (`session_id`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_229183_up') AS relationships;
relationships
Database: db_229183_up

Table Connections:
------------------
[Implied]  capabilities.user_id -> users.user_id
[Implied]  sessions.user_id -> users.user_id

DROP DATABASE `db_229183_up`;
UNINSTALL EXTENSION vsql_schematron;
