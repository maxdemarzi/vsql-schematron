INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 283454_join
#
CREATE DATABASE `db_283454_join`;
USE `db_283454_join`;
CREATE TABLE `empresas` (
`id` INT,
`nome` VARCHAR(64),
`endereco` VARCHAR(64),
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `historicos` (
`id` INT,
`fkempresa` INT,
`historico` VARCHAR(64),
`credito` DECIMAL(10,2),
`debito` DECIMAL(10,2),
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `funcionarios` (
`id` INT,
`fkempresa` INT,
`nome` VARCHAR(64),
`cargo` VARCHAR(64),
`salario` DECIMAL(10,2),
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_283454_join') AS relationships;
relationships
Database: db_283454_join

Table Connections:
------------------
(No relationships found)

DROP DATABASE `db_283454_join`;
UNINSTALL EXTENSION vsql_schematron;
