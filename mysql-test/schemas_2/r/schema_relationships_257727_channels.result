INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 257727_channels
#
CREATE DATABASE `db_257727_channels`;
USE `db_257727_channels`;
CREATE TABLE `channel_groups` (
`id` INT NOT NULL,
`name` TEXT,
`Reference` TEXT,
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `channels` (
`channel_group_id` INT,
`name` TEXT,
`type` TEXT,
`units` TEXT,
`low_cutoff` DOUBLE,
`high_cutoff` DOUBLE,
`reference` TEXT,
`groups` TEXT,
`description` TEXT,
`notch` DOUBLE,
`status` TEXT,
`status_description` TEXT
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_257727_channels') AS relationships;
relationships
Database: db_257727_channels

Table Connections:
------------------
[Implied]  channels.channel_group_id -> channel_groups.id

DROP DATABASE `db_257727_channels`;
UNINSTALL EXTENSION vsql_schematron;
