INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 223341_schema
#
CREATE DATABASE `db_223341_schema`;
USE `db_223341_schema`;
CREATE TABLE `category` (
`name` VARCHAR(50) NOT NULL,
PRIMARY KEY (`name`)
) CHARACTER SET latin1;
CREATE TABLE `transaction` (
`id` INT NOT NULL,
`amount` INT,
`type` INT,
`category` VARCHAR(50),
`description` VARCHAR(50),
`date` TIMESTAMP NULL,
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_223341_schema') AS relationships;
relationships
Database: db_223341_schema

Table Connections:
------------------
[Implied]  transaction.category -> category.name

DROP DATABASE `db_223341_schema`;
UNINSTALL EXTENSION vsql_schematron;
