INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 258018_find
#
CREATE DATABASE `db_258018_find`;
USE `db_258018_find`;
CREATE TABLE `record` (
`id` INT,
`find_id` INT,
`lost_id` INT,
`time` DATETIME,
`status` INT,
`way` INT,
`token` VARCHAR(64),
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `user` (
`id` INT,
`yyid` INT,
`number` VARCHAR(64),
`school` INT,
`name` VARCHAR(64),
`phone` VARCHAR(64),
`type` INT,
`blocked` INT,
`status` INT,
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_258018_find') AS relationships;
relationships
Database: db_258018_find

Table Connections:
------------------
[Implied]  record.id -> user.id

DROP DATABASE `db_258018_find`;
UNINSTALL EXTENSION vsql_schematron;
