INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 232388_install
--echo #
CREATE DATABASE `db_232388_install`;
USE `db_232388_install`;

CREATE TABLE `classes` (
  `class_id` INT,
  `name` VARCHAR(64),
  `teacher_id` INT,
  `prefect_id` INT,
  PRIMARY KEY (`class_id`)
) CHARACTER SET latin1;

CREATE TABLE `subjects` (
  `subject_id` INT,
  `name` VARCHAR(64),
  PRIMARY KEY (`subject_id`)
) CHARACTER SET latin1;

CREATE TABLE `teachers` (
  `teacher_id` INT,
  `hash` VARCHAR(64),
  `name` VARCHAR(64),
  `birthday` VARCHAR(64),
  `sex` VARCHAR(64),
  `religion` VARCHAR(64),
  `profileUrl` VARCHAR(64),
  `blood_group` VARCHAR(64),
  `role` VARCHAR(64),
  `address` VARCHAR(64),
  `phone` VARCHAR(64),
  `email` VARCHAR(64),
  `password` VARCHAR(64),
  `country` VARCHAR(64),
  `county` VARCHAR(64),
  `subjects` VARCHAR(64),
  `authentication_key` VARCHAR(64),
  PRIMARY KEY (`teacher_id`)
) CHARACTER SET latin1;

CREATE TABLE `subjectteacher` (
  `subject_id` INT,
  `teacher_id` INT
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_232388_install') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_232388_install') AS relationships;
DROP DATABASE `db_232388_install`;
UNINSTALL EXTENSION vsql_schematron;
