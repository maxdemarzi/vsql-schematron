INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 253929_V5__allow_null_job_input_data_file_size
--echo #
CREATE DATABASE `db_253929_V5__allow_null_job_input_data_file_size`;
USE `db_253929_V5__allow_null_job_input_data_file_size`;
CREATE TABLE `hungryhippos_tester.role` (
  `role_id` INT NOT NULL,
  `role` VARCHAR(50),
  PRIMARY KEY (`role_id`)
) CHARACTER SET latin1;
CREATE TABLE `hungryhippos_tester.user_role` (
  `user_role_id` INT NOT NULL,
  `user_id` INT,
  `role_id` INT,
  PRIMARY KEY (`user_role_id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_253929_V5__allow_null_job_input_data_file_size') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_253929_V5__allow_null_job_input_data_file_size') AS relationships;
DROP DATABASE `db_253929_V5__allow_null_job_input_data_file_size`;
UNINSTALL EXTENSION vsql_schematron;
