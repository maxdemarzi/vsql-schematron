INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 269589_701520575049
--echo #
CREATE DATABASE `db_269589_701520575049`;
USE `db_269589_701520575049`;

CREATE TABLE `admin_users` (
  `id` INT,
  `f_name` VARCHAR(64),
  `l_name` VARCHAR(64),
  `email` VARCHAR(64),
  `phone` VARCHAR(64),
  `image` VARCHAR(64),
  `password` VARCHAR(64),
  `remember_token` VARCHAR(64),
  `created_at` DATETIME,
  `updated_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `admin_user_roles` (
  `id` INT,
  `name` VARCHAR(64),
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `admin_user_role_assigns` (
  `id` INT,
  `user_id` INT,
  `role_id` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `allowances` (
  `id` INT,
  `payroll_id` INT,
  `name` VARCHAR(64),
  `amount` DECIMAL(10,2),
  `created_at` DATETIME,
  `updated_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `backup_allowances` (
  `id` INT,
  `backup_id` INT,
  `payroll_id` INT,
  `name` VARCHAR(64),
  `amount` DECIMAL(10,2),
  `status` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `backup_bookings` (
  `id` INT,
  `backup_id` INT,
  `user_id` INT,
  `name` VARCHAR(64),
  `phone` VARCHAR(64),
  `email` VARCHAR(64),
  `amount` DECIMAL(10,2),
  `advance` DECIMAL(10,2),
  `check_id` VARCHAR(64),
  `check_out` VARCHAR(64),
  `payment` VARCHAR(64),
  `status` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `backup_daily_expenses` (
  `id` INT,
  `backup_id` INT,
  `user_id` INT,
  `title` VARCHAR(64),
  `amount` DECIMAL(10,2),
  `date` VARCHAR(64),
  `status` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `backup_damage_products` (
  `id` INT,
  `backup_id` INT,
  `user_id` INT,
  `p_id` INT,
  `p_category_id` INT,
  `note` VARCHAR(64),
  `quantity` DECIMAL(10,2),
  `status` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `backup_deductions` (
  `id` INT,
  `backup_id` INT,
  `payroll_id` INT,
  `name` VARCHAR(64),
  `amount` DECIMAL(10,2),
  `status` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `backup_departments` (
  `id` INT,
  `backup_id` INT,
  `name` VARCHAR(64),
  `user_id` INT,
  `status` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `backup_designations` (
  `id` INT,
  `backup_id` INT,
  `name` VARCHAR(64),
  `user_id` INT,
  `status` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `backup_employees` (
  `id` INT,
  `backup_id` INT,
  `user_id` INT,
  `designation_id` INT,
  `department_id` INT,
  `f_name` VARCHAR(64),
  `l_name` VARCHAR(64),
  `pre_address` VARCHAR(64),
  `per_address` VARCHAR(64),
  `mobile` VARCHAR(64),
  `email` VARCHAR(64),
  `gender` VARCHAR(64),
  `image` VARCHAR(64),
  `em_id` INT,
  `basic_salary` DECIMAL(10,2),
  `status` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `backup_expenses` (
  `id` INT,
  `backup_id` INT,
  `user_id` INT,
  `expense_cat_id` INT,
  `title` VARCHAR(64),
  `amount` DECIMAL(10,2),
  `file` VARCHAR(64),
  `date` DATETIME,
  `status` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `backup_expense_categories` (
  `id` INT,
  `backup_id` INT,
  `user_id` INT,
  `name` VARCHAR(64),
  `status` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `backup_incomes` (
  `id` INT,
  `backup_id` INT,
  `user_id` INT,
  `income_cat_id` INT,
  `title` VARCHAR(64),
  `amount` DECIMAL(10,2),
  `date` DATETIME,
  `status` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `backup_income_categories` (
  `id` INT,
  `backup_id` INT,
  `user_id` INT,
  `name` VARCHAR(64),
  `status` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `backup_payrolls` (
  `id` INT,
  `backup_id` INT,
  `user_id` INT,
  `department_id` INT,
  `employee_id` INT,
  `month` VARCHAR(64),
  `year` VARCHAR(64),
  `basic_salary` DECIMAL(10,2),
  `total_allowance` DECIMAL(10,2),
  `total_deduction` DECIMAL(10,2),
  `net_salary` DECIMAL(10,2),
  `status` VARCHAR(64),
  `statuses` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `backup_products` (
  `id` INT,
  `backup_id` INT,
  `user_id` INT,
  `name` VARCHAR(64),
  `notify_quantity` INT,
  `p_category_id` INT,
  `status` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `backup_product_categories` (
  `id` INT,
  `backup_id` INT,
  `user_id` INT,
  `name` VARCHAR(64),
  `status` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `backup_purchases` (
  `id` INT,
  `backup_id` INT,
  `user_id` INT,
  `p_id` INT,
  `p_category_id` INT,
  `price` DECIMAL(10,2),
  `quantity` DECIMAL(10,2),
  `total_price` DECIMAL(10,2),
  `status` VARCHAR(64),
  `statuses` INT,
  `purchase_by` VARCHAR(64),
  `date` DATETIME,
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `backup_rooms` (
  `id` INT,
  `backup_id` INT,
  `user_id` INT,
  `category` VARCHAR(64),
  `bed_type` VARCHAR(64),
  `room_number` VARCHAR(64),
  `amount` DECIMAL(10,2),
  `facilities` VARCHAR(64),
  `status` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `bookings` (
  `id` INT,
  `room_number` INT,
  `book_no` VARCHAR(64),
  `name` VARCHAR(64),
  `phone` VARCHAR(64),
  `email` VARCHAR(64),
  `room_category` VARCHAR(64),
  `check_id` VARCHAR(64),
  `check_out` VARCHAR(64),
  `payment` VARCHAR(64),
  `amount` DECIMAL(10,2),
  `advance` DECIMAL(10,2),
  `user_id` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `booking_extra_facilities` (
  `id` INT,
  `book_id` INT,
  `date` VARCHAR(64),
  `name` VARCHAR(64),
  `amount` DECIMAL(10,2),
  `created_at` DATETIME,
  `updated_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `daily_deposites` (
  `id` INT,
  `amount` DECIMAL(10,2),
  `date` VARCHAR(64),
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `daily_expenses` (
  `id` INT,
  `title` VARCHAR(64),
  `amount` DECIMAL(10,2),
  `date` VARCHAR(64),
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `damage_products` (
  `id` INT,
  `p_category_id` INT,
  `p_id` INT,
  `quantity` DECIMAL(10,2),
  `note` VARCHAR(64),
  `created_at` DATETIME,
  `updated_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `deductions` (
  `id` INT,
  `payroll_id` INT,
  `name` VARCHAR(64),
  `amount` DECIMAL(10,2),
  `created_at` DATETIME,
  `updated_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `departments` (
  `id` INT,
  `name` VARCHAR(64),
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `designations` (
  `id` INT,
  `name` VARCHAR(64),
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `employees` (
  `id` INT,
  `designation_id` INT,
  `department_id` INT,
  `em_id` VARCHAR(64),
  `f_name` VARCHAR(64),
  `l_name` VARCHAR(64),
  `pre_address` VARCHAR(64),
  `per_address` VARCHAR(64),
  `mobile` VARCHAR(64),
  `email` VARCHAR(64),
  `image` VARCHAR(64),
  `gender` VARCHAR(64),
  `basic_salary` DECIMAL(10,2),
  `created_at` DATETIME,
  `updated_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `expenses` (
  `id` INT,
  `expense_cat_id` INT,
  `amount` DECIMAL(10,2),
  `title` VARCHAR(64),
  `file` VARCHAR(64),
  `date` DATETIME,
  `created_at` DATETIME,
  `updated_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `expense_categories` (
  `id` INT,
  `name` VARCHAR(64),
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `incomes` (
  `id` INT,
  `income_cat_id` INT,
  `amount` DECIMAL(10,2),
  `title` VARCHAR(64),
  `date` DATETIME,
  `created_at` DATETIME,
  `updated_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `income_categories` (
  `id` INT,
  `name` VARCHAR(64),
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `invoice_amounts` (
  `id` INT,
  `book_id` INT,
  `subtotal` DECIMAL(10,2),
  `discount` VARCHAR(64),
  `advance` DECIMAL(10,2),
  `total` DECIMAL(10,2),
  `status` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `migrations` (
  `id` INT,
  `migration` VARCHAR(64),
  `batch` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `password_resets` (
  `email` VARCHAR(64),
  `token` VARCHAR(64),
  `created_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `payrolls` (
  `id` INT,
  `department_id` INT,
  `employee_id` INT,
  `month` VARCHAR(64),
  `year` VARCHAR(64),
  `basic_salary` DECIMAL(10,2),
  `total_allowance` DECIMAL(10,2),
  `total_deduction` DECIMAL(10,2),
  `net_salary` DECIMAL(10,2),
  `status` VARCHAR(64),
  `created_at` DATETIME,
  `updated_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `products` (
  `id` INT,
  `p_category_id` INT,
  `name` VARCHAR(64),
  `notify_quantity` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `product_categories` (
  `id` INT,
  `name` VARCHAR(64),
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `purchases` (
  `id` INT,
  `p_category_id` INT,
  `p_id` INT,
  `price` DECIMAL(10,2),
  `quantity` DECIMAL(10,2),
  `total_price` DECIMAL(10,2),
  `status` VARCHAR(64),
  `purchase_by` VARCHAR(64),
  `date` DATETIME,
  `created_at` DATETIME,
  `updated_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `purchase_expense` (
  `id` INT,
  `purchase_id` INT,
  `expense_id` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `rooms` (
  `id` INT,
  `category` VARCHAR(64),
  `bed_type` VARCHAR(64),
  `room_number` VARCHAR(64),
  `amount` DECIMAL(10,2),
  `facilities` VARCHAR(64),
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `room_numbers` (
  `id` INT,
  `room_id` INT,
  `room_number` VARCHAR(64),
  `created_at` DATETIME,
  `updated_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `sent_products` (
  `id` INT,
  `p_id` INT,
  `outside_quantity` DECIMAL(10,2),
  `receive_quantity` DECIMAL(10,2),
  `status` VARCHAR(64),
  `created_at` DATETIME,
  `updated_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `users` (
  `id` INT,
  `name` VARCHAR(64),
  `email` VARCHAR(64),
  `password` VARCHAR(64),
  `remember_token` VARCHAR(64),
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `use_products` (
  `id` INT,
  `p_id` INT,
  `quantity` INT,
  `created_at` DATETIME,
  `updated_at` DATETIME
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_269589_701520575049') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_269589_701520575049') AS relationships;
DROP DATABASE `db_269589_701520575049`;
UNINSTALL EXTENSION vsql_schematron;
