INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 665140_migration
--echo #
CREATE DATABASE `db_665140_migration`;
USE `db_665140_migration`;
CREATE TABLE `Admin` (
  `id` INT NOT NULL,
  `firstName` TEXT,
  `lastName` TEXT,
  `userName` TEXT,
  `password` TEXT,
  `status` TEXT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `Menu` (
  `id` INT NOT NULL,
  `price` DECIMAL(10,2),
  `startDate` DATETIME,
  `endDate` DATETIME,
  `foodId` INT,
  `adminId` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `FoodItem` (
  `id` INT NOT NULL,
  `name` TEXT,
  `quantity` INT,
  `unitPrice` DECIMAL(10,2),
  `categoryId` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `Category` (
  `id` INT NOT NULL,
  `name` TEXT,
  `note` TEXT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_665140_migration') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_665140_migration') AS relationships;
DROP DATABASE `db_665140_migration`;
UNINSTALL EXTENSION vsql_schematron;
