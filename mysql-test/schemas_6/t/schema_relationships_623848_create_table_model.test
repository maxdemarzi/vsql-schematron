INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 623848_create_table_model
--echo #
CREATE DATABASE `db_623848_create_table_model`;
USE `db_623848_create_table_model`;

CREATE TABLE `org.Contact` (
  `ContactId` VARCHAR(64),
  `DateOfBirth` DATETIME,
  `FirstName` VARCHAR(64),
  `LastName` VARCHAR(64),
  `HeightFt` INT,
  `HeightInch` INT,
  `Picture` VARCHAR(64),
  `PreferredHeight` VARCHAR(64),
  PRIMARY KEY (`ContactId`)
) CHARACTER SET latin1;

CREATE TABLE `org.Organization` (
  `OrganizationId` VARCHAR(64),
  `Name` VARCHAR(64),
  PRIMARY KEY (`OrganizationId`)
) CHARACTER SET latin1;

CREATE TABLE `org.WorkHistory` (
  `WorkHistoryId` VARCHAR(64),
  `ContactId` VARCHAR(64),
  `OrganizationId` VARCHAR(64),
  `StartMonth` INT,
  `StartYear` INT,
  `EndMonth` INT,
  `EndYear` INT,
  `JobDescription` VARCHAR(64),
  `JobTitle` VARCHAR(64),
  PRIMARY KEY (`WorkHistoryId`)
) CHARACTER SET latin1;

CREATE TABLE `org.ContactLink` (
  `ContactLinkId` VARCHAR(64),
  `SourceContactId` VARCHAR(64),
  `TargetContactId` VARCHAR(64),
  `WorkHistoryId` VARCHAR(64),
  PRIMARY KEY (`ContactLinkId`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_623848_create_table_model') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_623848_create_table_model') AS relationships;
DROP DATABASE `db_623848_create_table_model`;
UNINSTALL EXTENSION vsql_schematron;
