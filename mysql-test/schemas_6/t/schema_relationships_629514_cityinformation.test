INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 629514_cityinformation
--echo #
CREATE DATABASE `db_629514_cityinformation`;
USE `db_629514_cityinformation`;
CREATE TABLE `grad` (
  `imeGrad` VARCHAR(50),
  `idGrad` INT NOT NULL,
  `idŽupanija` INT,
  PRIMARY KEY (`idGrad`)
) CHARACTER SET latin1;
CREATE TABLE `recenzija` (
  `idRecenzija` INT NOT NULL,
  `tekstRecenzija` VARCHAR(50),
  `vlasnikRecenzijaID` VARCHAR(50),
  `odobrenoRecenzija` TINYINT,
  `datumRecenzija` VARCHAR(50),
  `poduzećeRecenzijaID` VARCHAR(50),
  PRIMARY KEY (`idRecenzija`)
) CHARACTER SET latin1;
CREATE TABLE `slika` (
  `idSlika` VARCHAR(50) NOT NULL,
  `putanjaSlike` VARCHAR(50),
  `idPoduzeća` VARCHAR(50),
  PRIMARY KEY (`idSlika`)
) CHARACTER SET latin1;
CREATE TABLE `ulica` (
  `idUlica` INT NOT NULL,
  `imeUlica` VARCHAR(50),
  `idGrad` INT,
  PRIMARY KEY (`idUlica`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_629514_cityinformation') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_629514_cityinformation') AS relationships;
DROP DATABASE `db_629514_cityinformation`;
UNINSTALL EXTENSION vsql_schematron;
