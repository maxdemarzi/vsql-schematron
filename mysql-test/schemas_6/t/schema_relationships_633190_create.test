INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 633190_create
--echo #
CREATE DATABASE `db_633190_create`;
USE `db_633190_create`;
CREATE TABLE `oauth2_access_tokens` (
  `access_token` VARCHAR(50) NOT NULL,
  `client_id` VARCHAR(50),
  `expires_at` DATETIME,
  `scopes` TEXT,
  `resource_owner_id` VARCHAR(50),
  `resource_owner_type` VARCHAR(50),
  PRIMARY KEY (`access_token`)
) CHARACTER SET latin1;
CREATE TABLE `oauth2_clients` (
  `client_id` VARCHAR(50) NOT NULL,
  `client_secret` VARCHAR(50),
  `redirect_uris` TEXT,
  `allowed_grant_types` TEXT,
  PRIMARY KEY (`client_id`)
) CHARACTER SET latin1;
CREATE TABLE `oauth2_refresh_tokens` (
  `refresh_token` VARCHAR(50) NOT NULL,
  `associated_access_token` VARCHAR(50),
  `expires_at` DATETIME,
  `revoked_at` DATETIME,
  PRIMARY KEY (`refresh_token`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_633190_create') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_633190_create') AS relationships;
DROP DATABASE `db_633190_create`;
UNINSTALL EXTENSION vsql_schematron;
