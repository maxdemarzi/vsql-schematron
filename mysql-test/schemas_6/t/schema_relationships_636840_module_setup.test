INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 636840_module_setup
--echo #
CREATE DATABASE `db_636840_module_setup`;
USE `db_636840_module_setup`;

CREATE TABLE `oauth2_idp_persons` (
  `person_id` INT,
  `first_name` VARCHAR(64),
  `last_name` VARCHAR(64),
  `email_address` VARCHAR(64),
  `cell_number` VARCHAR(64),
  `phone_number` VARCHAR(64),
  PRIMARY KEY (`person_id`)
) CHARACTER SET latin1;

CREATE TABLE `oauth2_idp_users` (
  `user_id` INT,
  `username` VARCHAR(64),
  `password` VARCHAR(64),
  `person_id` INT,
  PRIMARY KEY (`user_id`)
) CHARACTER SET latin1;

CREATE TABLE `oauth2_clients` (
  `client_id` VARCHAR(64),
  `name` VARCHAR(64),
  `description` VARCHAR(64),
  `grant_types` VARCHAR(64),
  `token_iv` VARCHAR(64),
  `token_key` VARCHAR(64),
  `revoked` VARCHAR(64),
  PRIMARY KEY (`client_id`)
) CHARACTER SET latin1;

CREATE TABLE `oauth2_client_returnurls` (
  `url_id` INT,
  `client_id` VARCHAR(64),
  `return_url` VARCHAR(64),
  PRIMARY KEY (`url_id`)
) CHARACTER SET latin1;

CREATE TABLE `oauth2_scopes` (
  `scope_id` INT,
  `name` VARCHAR(64),
  `description` VARCHAR(64),
  PRIMARY KEY (`scope_id`)
) CHARACTER SET latin1;

CREATE TABLE `oauth2_client_scopes` (
  `client_id` VARCHAR(64),
  `scope_id` INT,
  PRIMARY KEY (`client_id`, `scope_id`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_636840_module_setup') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_636840_module_setup') AS relationships;
DROP DATABASE `db_636840_module_setup`;
UNINSTALL EXTENSION vsql_schematron;
