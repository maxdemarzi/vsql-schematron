INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 684670_mecanicaAutomoveis
#
CREATE DATABASE `db_684670_mecanicaAutomoveis`;
USE `db_684670_mecanicaAutomoveis`;
CREATE TABLE `ordem_servico` (
`numero_OS` INT,
`data_emissao` DATETIME,
`data_conclusao` DATETIME,
`valor` DECIMAL(10,2),
`descricao` VARCHAR(64),
PRIMARY KEY (`numero_OS`)
) CHARACTER SET latin1;
CREATE TABLE `pessoa` (
`codigo` INT,
`nome` VARCHAR(64),
`cpf` VARCHAR(64),
`endereco` VARCHAR(64),
PRIMARY KEY (`codigo`)
) CHARACTER SET latin1;
CREATE TABLE `proprietario` (
`codigo_proprietario` INT,
`email` VARCHAR(64),
`telefone` VARCHAR(64),
PRIMARY KEY (`codigo_proprietario`)
) CHARACTER SET latin1;
CREATE TABLE `mecanico` (
`codigo_mecanico` INT,
`numero_OS` INT,
`especialidade` VARCHAR(64),
PRIMARY KEY (`codigo_mecanico`)
) CHARACTER SET latin1;
CREATE TABLE `veiculo` (
`codigo_veiculo` INT,
`descricao` VARCHAR(64),
`placa` VARCHAR(64),
`codigo_proprietario` INT,
`numero_OS` INT,
PRIMARY KEY (`codigo_veiculo`)
) CHARACTER SET latin1;
CREATE TABLE `peca` (
`numero_OS` INT,
`id` INT,
`tamanho` VARCHAR(64),
`preco` DECIMAL(10,2),
PRIMARY KEY (`numero_OS`, `id`)
) CHARACTER SET latin1;
CREATE TABLE `requer` (
`numero_OS` INT,
`id_peca` INT,
PRIMARY KEY (`numero_OS`, `id_peca`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_684670_mecanicaAutomoveis') AS relationships;
relationships
Database: db_684670_mecanicaAutomoveis

Table Connections:
------------------
[Implied]  mecanico.numero_OS -> ordem_servico.numero_OS
[Implied]  peca.numero_OS -> ordem_servico.numero_OS
[Implied]  requer.numero_OS -> ordem_servico.numero_OS
[Implied]  veiculo.codigo_proprietario -> proprietario.codigo_proprietario
[Implied]  veiculo.numero_OS -> ordem_servico.numero_OS

DROP DATABASE `db_684670_mecanicaAutomoveis`;
UNINSTALL EXTENSION vsql_schematron;
