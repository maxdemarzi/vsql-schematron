INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 636191_demo
#
CREATE DATABASE `db_636191_demo`;
USE `db_636191_demo`;
CREATE TABLE `collect` (
`username` VARCHAR(255),
`file_id` INT
) CHARACTER SET latin1;
CREATE TABLE `favourite` (
`username` VARCHAR(255),
`file_id` INT,
`y_n` INT
) CHARACTER SET latin1;
CREATE TABLE `files` (
`id` INT,
`filename` VARCHAR(255),
`path` VARCHAR(255),
`username` VARCHAR(255),
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `file_description` (
`username` VARCHAR(255),
`file_id` INT,
`description` VARCHAR(255),
`id` INT,
`ip_address` VARCHAR(255),
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `sample_data` (
`id` INT,
`first_name` VARCHAR(255),
`last_name` VARCHAR(255),
`age` VARCHAR(255),
`gender` VARCHAR(255),
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `users` (
`username` VARCHAR(255),
`password` VARCHAR(255),
`email` VARCHAR(255),
`r_date` DATETIME,
`is_confirm` INT,
`phone_num` INT,
`path` VARCHAR(255),
PRIMARY KEY (`username`)
) CHARACTER SET latin1;
CREATE TABLE `user_avatar` (
`username` VARCHAR(255),
`is_set` INT,
`path` VARCHAR(255),
PRIMARY KEY (`username`)
) CHARACTER SET latin1;
CREATE TABLE `videos` (
`title` VARCHAR(255),
`username` VARCHAR(255),
`tag` VARCHAR(255),
`videoid` INT
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_636191_demo') AS relationships;
relationships
Database: db_636191_demo

Table Connections:
------------------
[Implied]  collect.file_id -> files.id
[Implied]  collect.username -> user_avatar.username
[Implied]  collect.username -> users.username
[Implied]  favourite.file_id -> files.id
[Implied]  favourite.username -> user_avatar.username
[Implied]  favourite.username -> users.username
[Implied]  file_description.file_id -> files.id
[Implied]  file_description.username -> user_avatar.username
[Implied]  file_description.username -> users.username
[Implied]  files.username -> user_avatar.username
[Implied]  files.username -> users.username
[Implied]  user_avatar.username -> users.username
[Implied]  videos.username -> user_avatar.username
[Implied]  videos.username -> users.username

DROP DATABASE `db_636191_demo`;
UNINSTALL EXTENSION vsql_schematron;
