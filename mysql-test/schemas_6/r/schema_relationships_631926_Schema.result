INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 631926_Schema
#
CREATE DATABASE `db_631926_Schema`;
USE `db_631926_Schema`;
CREATE TABLE `travelers` (
`traveler_id` INT,
`name` VARCHAR(64),
`photo_url` VARCHAR(64),
`points` INT
) CHARACTER SET latin1;
CREATE TABLE `places` (
`place_id` INT,
`name` VARCHAR(64),
`beenHere` INT,
`wantToGo` INT,
`photoURL` VARCHAR(64),
`description` VARCHAR(64),
`date` VARCHAR(64),
`points` INT
) CHARACTER SET latin1;
CREATE TABLE `friends` (
`friend1` INT,
`friend2` INT
) CHARACTER SET latin1;
CREATE TABLE `traveler_places` (
`traveler` INT,
`place` INT,
`date` VARCHAR(64)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_631926_Schema') AS relationships;
relationships
Database: db_631926_Schema

Table Connections:
------------------
[Implied]  traveler_places.place -> places.place_id
[Implied]  traveler_places.traveler -> travelers.traveler_id

DROP DATABASE `db_631926_Schema`;
UNINSTALL EXTENSION vsql_schematron;
