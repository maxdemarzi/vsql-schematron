INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 354573_newtable
#
CREATE DATABASE `db_354573_newtable`;
USE `db_354573_newtable`;
CREATE TABLE `users` (
`id` INT,
`username` VARCHAR(64),
`pwd` VARCHAR(64),
`auth` INT,
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `word` (
`id` INT,
`kana` VARCHAR(64),
`kanji` VARCHAR(64),
`type` VARCHAR(64),
`trans` VARCHAR(64),
`lid` INT,
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `record` (
`id` INT,
`wid` INT,
`uid` INT,
`total_record` INT,
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_354573_newtable') AS relationships;
relationships
Database: db_354573_newtable

Table Connections:
------------------
(No relationships found)

DROP DATABASE `db_354573_newtable`;
UNINSTALL EXTENSION vsql_schematron;
