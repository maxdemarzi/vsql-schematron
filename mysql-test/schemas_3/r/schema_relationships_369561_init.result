INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 369561_init
#
CREATE DATABASE `db_369561_init`;
USE `db_369561_init`;
CREATE TABLE `User` (
`UID` INT,
`username` VARCHAR(64),
`password` VARCHAR(64),
PRIMARY KEY (`UID`)
) CHARACTER SET latin1;
CREATE TABLE `Memos` (
`ID` INT,
`UID` INT,
`deadline` DATETIME,
`headline` VARCHAR(64),
`detail` VARCHAR(64),
PRIMARY KEY (`ID`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_369561_init') AS relationships;
relationships
Database: db_369561_init

Table Connections:
------------------
(No relationships found)

DROP DATABASE `db_369561_init`;
UNINSTALL EXTENSION vsql_schematron;
