INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 322968_SQLDump
#
CREATE DATABASE `db_322968_SQLDump`;
USE `db_322968_SQLDump`;
CREATE TABLE `PHONE` (
`ID` INT,
`TYPE` VARCHAR(64),
`NUM` VARCHAR(64),
PRIMARY KEY (`ID`)
) CHARACTER SET latin1;
CREATE TABLE `PARKING_SPACE` (
`ID` INT,
`LOT` INT,
`LOCATION` VARCHAR(64),
PRIMARY KEY (`ID`)
) CHARACTER SET latin1;
CREATE TABLE `DEPARTMENT` (
`ID` INT,
`NAME` VARCHAR(64),
PRIMARY KEY (`ID`)
) CHARACTER SET latin1;
CREATE TABLE `EMPLOYEE` (
`ID` INT,
`NAME` VARCHAR(64),
`SALARY` INT,
`PSPACE_ID` INT,
`DEPARTMENT_ID` INT,
PRIMARY KEY (`ID`)
) CHARACTER SET latin1;
CREATE TABLE `EMP_PHONE` (
`PHONE_ID` INT,
`EMP_ID` INT,
PRIMARY KEY (`PHONE_ID`, `EMP_ID`)
) CHARACTER SET latin1;
CREATE TABLE `EMP_PROJ` (
`PROJ_ID` INT,
`EMP_ID` INT,
PRIMARY KEY (`PROJ_ID`, `EMP_ID`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_322968_SQLDump') AS relationships;
relationships
Database: db_322968_SQLDump

Table Connections:
------------------
[Implied]  EMPLOYEE.DEPARTMENT_ID -> DEPARTMENT.ID
[Implied]  EMP_PHONE.EMP_ID -> EMPLOYEE.ID
[Implied]  EMP_PHONE.PHONE_ID -> PHONE.ID
[Implied]  EMP_PROJ.EMP_ID -> EMPLOYEE.ID

DROP DATABASE `db_322968_SQLDump`;
UNINSTALL EXTENSION vsql_schematron;
