INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 362391_database
--echo #
CREATE DATABASE `db_362391_database`;
USE `db_362391_database`;

CREATE TABLE `addresses` (
  `id` INT,
  `line` VARCHAR(255),
  `city` VARCHAR(255),
  `state` VARCHAR(255),
  `country` VARCHAR(255),
  `phone` VARCHAR(255),
  `user_id` INT
) CHARACTER SET latin1;

CREATE TABLE `categories` (
  `id` INT,
  `title` VARCHAR(255),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `orders` (
  `id` INT,
  `user_id` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `orders_details` (
  `id` INT,
  `order_id` INT,
  `product_id` INT,
  `quantity` INT
) CHARACTER SET latin1;

CREATE TABLE `products` (
  `id` INT,
  `title` VARCHAR(255),
  `image` VARCHAR(255),
  `images` VARCHAR(255),
  `description` VARCHAR(255),
  `price` DECIMAL(10,2),
  `quantity` INT,
  `cat_id` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `users` (
  `id` INT,
  `username` VARCHAR(255),
  `password` VARCHAR(255),
  `email` VARCHAR(255),
  `fname` VARCHAR(255),
  `lname` VARCHAR(255),
  `age` INT,
  `role` INT,
  `type` VARCHAR(255),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_362391_database') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_362391_database') AS relationships;
DROP DATABASE `db_362391_database`;
UNINSTALL EXTENSION vsql_schematron;
