INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 398883_schema
--echo #
CREATE DATABASE `db_398883_schema`;
USE `db_398883_schema`;

CREATE TABLE `departments` (
  `department_id` INT,
  `department_name` VARCHAR(64),
  PRIMARY KEY (`department_id`)
) CHARACTER SET latin1;

CREATE TABLE `roles` (
  `role_id` INT,
  `title` VARCHAR(64),
  `salary` DECIMAL(10,2),
  `department_id` INT,
  PRIMARY KEY (`role_id`)
) CHARACTER SET latin1;

CREATE TABLE `employees` (
  `employee_id` INT,
  `first_name` VARCHAR(64),
  `last_name` VARCHAR(64),
  `role_id` INT,
  `manager_id` INT,
  PRIMARY KEY (`employee_id`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_398883_schema') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_398883_schema') AS relationships;
DROP DATABASE `db_398883_schema`;
UNINSTALL EXTENSION vsql_schematron;
