INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 333743_complete_v3
--echo #
CREATE DATABASE `db_333743_complete_v3`;
USE `db_333743_complete_v3`;

CREATE TABLE `DATABASE_VERSION` (
  `version` INT
) CHARACTER SET latin1;

CREATE TABLE `HOUSE` (
  `id` VARCHAR(255),
  `name` VARCHAR(255),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `DEVICE` (
  `id` VARCHAR(255),
  `name` VARCHAR(255),
  `houseid` VARCHAR(255),
  `last_ping` DATETIME,
  `last_restart` DATETIME,
  `last_watchdog_reset` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `LOGIN_USER` (
  `id` VARCHAR(255),
  `google_sub` VARCHAR(255),
  `email` VARCHAR(255),
  `fn` VARCHAR(255),
  `ln` VARCHAR(255),
  `default_notify_using` VARCHAR(255),
  `pushover_userkey` VARCHAR(255),
  `pushover_apptoken` VARCHAR(255),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `DEVICE_WATCHDOG` (
  `userId` VARCHAR(255),
  `deviceId` VARCHAR(255),
  `notify` VARCHAR(255),
  `muted_until` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `SENSOR` (
  `id` VARCHAR(255),
  `name` VARCHAR(255),
  `deviceid` VARCHAR(255),
  `type` VARCHAR(255),
  `label` VARCHAR(255),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `SENSOR_DATA` (
  `id` VARCHAR(255),
  `dt` DATETIME,
  `value` DECIMAL(10,2)
) CHARACTER SET latin1;

CREATE TABLE `FAVORITE_SENSOR` (
  `userId` VARCHAR(255),
  `sensorId` VARCHAR(255),
  PRIMARY KEY (`userId`, `sensorId`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_333743_complete_v3') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_333743_complete_v3') AS relationships;
DROP DATABASE `db_333743_complete_v3`;
UNINSTALL EXTENSION vsql_schematron;
