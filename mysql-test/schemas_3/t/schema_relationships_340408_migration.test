INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 340408_migration
--echo #
CREATE DATABASE `db_340408_migration`;
USE `db_340408_migration`;

CREATE TABLE `accounts` (
  `id` VARCHAR(64),
  `authentication_id` VARCHAR(64),
  `name` VARCHAR(64),
  `email` VARCHAR(64),
  `last_name` VARCHAR(64),
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `authentications` (
  `id` VARCHAR(64),
  `account_id` VARCHAR(64),
  `sessions_number` INT,
  `password` VARCHAR(64),
  `failed_attempts` INT,
  `locked` VARCHAR(64),
  `status` VARCHAR(64),
  `active` VARCHAR(64),
  `auth_groups_id` VARCHAR(64),
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `authentication__sessions` (
  `authentication_id` VARCHAR(64),
  `session_id` VARCHAR(64),
  PRIMARY KEY (`authentication_id`, `session_id`)
) CHARACTER SET latin1;

CREATE TABLE `sessions` (
  `id` VARCHAR(64),
  `ip` VARCHAR(64),
  `device` VARCHAR(64),
  `active` VARCHAR(64),
  `created_at` DATETIME,
  `expires_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `auth_groups` (
  `id` VARCHAR(64),
  `title` VARCHAR(64),
  `created_at` DATETIME,
  `active` VARCHAR(64),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `auth_groups__permissions` (
  `auth_group_id` VARCHAR(64),
  `permission_id` VARCHAR(64),
  PRIMARY KEY (`auth_group_id`, `permission_id`)
) CHARACTER SET latin1;

CREATE TABLE `permissions` (
  `name` VARCHAR(64),
  `action` VARCHAR(64),
  PRIMARY KEY (`name`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_340408_migration') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_340408_migration') AS relationships;
DROP DATABASE `db_340408_migration`;
UNINSTALL EXTENSION vsql_schematron;
