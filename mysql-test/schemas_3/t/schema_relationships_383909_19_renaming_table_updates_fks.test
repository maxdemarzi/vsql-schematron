INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 383909_19-renaming-table-updates-fks
--echo #
CREATE DATABASE `db_383909_19_renaming_table_updates_fks`;
USE `db_383909_19_renaming_table_updates_fks`;

CREATE TABLE `aaa` (
  `id` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `bbb` (
  `a` INT
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_383909_19_renaming_table_updates_fks') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_383909_19_renaming_table_updates_fks') AS relationships;
DROP DATABASE `db_383909_19_renaming_table_updates_fks`;
UNINSTALL EXTENSION vsql_schematron;
