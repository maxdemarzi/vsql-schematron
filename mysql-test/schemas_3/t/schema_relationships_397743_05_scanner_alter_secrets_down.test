INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 397743_05_scanner_alter_secrets.down
--echo #
CREATE DATABASE `db_397743_05_scanner_alter_secrets_down`;
USE `db_397743_05_scanner_alter_secrets_down`;
CREATE TABLE `secrets` (
  `id` VARCHAR(50) NOT NULL,
  `name` TEXT,
  `type` TEXT,
  `last_modified` TIMESTAMP NULL,
  `data` TEXT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `secrets_tags` (
  `secret_id` TEXT,
  `tag_id` TEXT
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_397743_05_scanner_alter_secrets_down') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_397743_05_scanner_alter_secrets_down') AS relationships;
DROP DATABASE `db_397743_05_scanner_alter_secrets_down`;
UNINSTALL EXTENSION vsql_schematron;
