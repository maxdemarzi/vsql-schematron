INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 364465_DomainUsedInPKThatsUsedInFK_00.Target
--echo #
CREATE DATABASE `db_364465_DomainUsedInPKThatsUsedInFK_00_Target`;
USE `db_364465_DomainUsedInPKThatsUsedInFK_00_Target`;

CREATE TABLE `b` (
  `i` DECIMAL(10,2),
  PRIMARY KEY (`i`)
) CHARACTER SET latin1;

CREATE TABLE `a` (
  `i` DECIMAL(10,2),
  PRIMARY KEY (`i`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_364465_DomainUsedInPKThatsUsedInFK_00_Target') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_364465_DomainUsedInPKThatsUsedInFK_00_Target') AS relationships;
DROP DATABASE `db_364465_DomainUsedInPKThatsUsedInFK_00_Target`;
UNINSTALL EXTENSION vsql_schematron;
