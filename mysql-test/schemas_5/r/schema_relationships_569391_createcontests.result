INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 569391_createcontests
#
CREATE DATABASE `db_569391_createcontests`;
USE `db_569391_createcontests`;
CREATE TABLE `RECLEAGUE.CONTEST_INFO` (
`CONTEST_ID` DECIMAL(10,2),
`CONTEST_NAME` VARCHAR(255),
`DESCRIPTION` VARCHAR(255),
PRIMARY KEY (`CONTEST_ID`)
) CHARACTER SET latin1;
CREATE TABLE `RECLEAGUE.CONTESTS` (
`EVENT_ID` DECIMAL(10,2),
`CONTEST_ID` DECIMAL(10,2),
PRIMARY KEY (`CONTEST_ID`, `EVENT_ID`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_569391_createcontests') AS relationships;
relationships
Database: db_569391_createcontests

Table Connections:
------------------
[Implied]  RECLEAGUE.CONTESTS.CONTEST_ID -> RECLEAGUE.CONTEST_INFO.CONTEST_ID

DROP DATABASE `db_569391_createcontests`;
UNINSTALL EXTENSION vsql_schematron;
