INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 559556_createTables
#
CREATE DATABASE `db_559556_createTables`;
USE `db_559556_createTables`;
CREATE TABLE `skill` (
`skillid` INT,
`skillname` VARCHAR(64),
PRIMARY KEY (`skillid`)
) CHARACTER SET latin1;
CREATE TABLE `curriculum` (
`curriculumid` INT,
`createdby` VARCHAR(64),
`createdon` DATETIME,
`lastmodified` DATETIME,
`lastmodifiedby` VARCHAR(64),
`curriculumname` VARCHAR(64),
PRIMARY KEY (`curriculumid`)
) CHARACTER SET latin1;
CREATE TABLE `trainer` (
`trainerid` INT,
`email` VARCHAR(64),
`trainerfirst` VARCHAR(64),
`trainerlast` VARCHAR(64),
PRIMARY KEY (`trainerid`)
) CHARACTER SET latin1;
CREATE TABLE `client` (
`clientid` INT,
`clientname` VARCHAR(64),
PRIMARY KEY (`clientid`)
) CHARACTER SET latin1;
CREATE TABLE `batch` (
`batchid` INT,
`batchsize` INT,
`enddate` DATETIME,
`startdate` DATETIME,
`trainerid` INT,
`clientid` INT,
`confirmed` VARCHAR(64),
`curriculumid` INT,
PRIMARY KEY (`batchid`)
) CHARACTER SET latin1;
CREATE TABLE `demand` (
`demandid` INT,
`clientid` INT,
`curriculumid` INT,
`needby` DATETIME,
`quantitydemanded` INT,
PRIMARY KEY (`demandid`)
) CHARACTER SET latin1;
CREATE TABLE `curriculum_skill` (
`skillid` INT,
`curriculumid` INT
) CHARACTER SET latin1;
CREATE TABLE `trainer_skill` (
`trainerid` INT,
`skillid` INT
) CHARACTER SET latin1;
CREATE TABLE `trainer_curriculum` (
`trainerid` INT,
`curriculumid` INT
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_559556_createTables') AS relationships;
relationships
Database: db_559556_createTables

Table Connections:
------------------
[Implied]  batch.clientid -> client.clientid
[Implied]  batch.curriculumid -> curriculum.curriculumid
[Implied]  batch.trainerid -> trainer.trainerid
[Implied]  curriculum_skill.curriculumid -> curriculum.curriculumid
[Implied]  curriculum_skill.skillid -> skill.skillid
[Implied]  demand.clientid -> client.clientid
[Implied]  demand.curriculumid -> curriculum.curriculumid
[Implied]  trainer_curriculum.curriculumid -> curriculum.curriculumid
[Implied]  trainer_curriculum.trainerid -> trainer.trainerid
[Implied]  trainer_skill.skillid -> skill.skillid
[Implied]  trainer_skill.trainerid -> trainer.trainerid

DROP DATABASE `db_559556_createTables`;
UNINSTALL EXTENSION vsql_schematron;
