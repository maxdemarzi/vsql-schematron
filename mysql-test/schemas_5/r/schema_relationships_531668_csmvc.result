INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 531668_csmvc
#
CREATE DATABASE `db_531668_csmvc`;
USE `db_531668_csmvc`;
CREATE TABLE `pemain` (
`id` INT,
`nama` VARCHAR(50),
`posisi_id` INT,
`negara` VARCHAR(50),
`keterangan` VARCHAR(50)
) CHARACTER SET latin1;
CREATE TABLE `posisi` (
`id` INT,
`nama` VARCHAR(50),
`keterangan` VARCHAR(50)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_531668_csmvc') AS relationships;
relationships
Database: db_531668_csmvc

Table Connections:
------------------
[Implied]  pemain.posisi_id -> posisi.id

DROP DATABASE `db_531668_csmvc`;
UNINSTALL EXTENSION vsql_schematron;
