INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 575496_Create_Products
#
CREATE DATABASE `db_575496_Create_Products`;
USE `db_575496_Create_Products`;
CREATE TABLE `product_type` (
`id` DECIMAL(10,2),
`name` VARCHAR(64),
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `product` (
`id` DECIMAL(10,2),
`name` VARCHAR(64),
`type_id` DECIMAL(10,2),
`expired_date` DATETIME,
`price` DECIMAL(10,2),
`quantity` DECIMAL(10,2),
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_575496_Create_Products') AS relationships;
relationships
Database: db_575496_Create_Products

Table Connections:
------------------
[Implied]  product.type_id -> product_type.id

DROP DATABASE `db_575496_Create_Products`;
UNINSTALL EXTENSION vsql_schematron;
