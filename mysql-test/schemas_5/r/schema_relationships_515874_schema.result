INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 515874_schema
#
CREATE DATABASE `db_515874_schema`;
USE `db_515874_schema`;
CREATE TABLE `USERS` (
`username` VARCHAR(50) NOT NULL,
`password` VARCHAR(50),
`enabled` TINYINT,
PRIMARY KEY (`username`)
) CHARACTER SET latin1;
CREATE TABLE `AUTHORITIES` (
`authority` VARCHAR(50),
`username` VARCHAR(50)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_515874_schema') AS relationships;
relationships
Database: db_515874_schema

Table Connections:
------------------
[Implied]  AUTHORITIES.username -> USERS.username

DROP DATABASE `db_515874_schema`;
UNINSTALL EXTENSION vsql_schematron;
