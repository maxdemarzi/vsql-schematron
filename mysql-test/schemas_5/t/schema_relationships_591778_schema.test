INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 591778_schema
--echo #
CREATE DATABASE `db_591778_schema`;
USE `db_591778_schema`;
CREATE TABLE `user` (
  `id` INT NOT NULL,
  `username` TEXT,
  `password` TEXT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `payment` (
  `id` INT NOT NULL,
  `user_id` INT,
  `amount` DECIMAL(10,2),
  `description` TEXT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_591778_schema') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_591778_schema') AS relationships;
DROP DATABASE `db_591778_schema`;
UNINSTALL EXTENSION vsql_schematron;
