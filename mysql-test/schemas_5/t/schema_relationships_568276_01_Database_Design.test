INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 568276_01_Database_Design
--echo #
CREATE DATABASE `db_568276_01_Database_Design`;
USE `db_568276_01_Database_Design`;

CREATE TABLE `credentials` (
  `id` INT,
  `email` VARCHAR(64),
  `password` VARCHAR(64),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `locations` (
  `id` INT,
  `latitude` DECIMAL(10,2),
  `longitude` DECIMAL(10,2),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `users` (
  `id` INT,
  `nickname` VARCHAR(64),
  `gender` VARCHAR(64),
  `age` INT,
  `location_id` INT,
  `credential_id` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `chats` (
  `id` INT,
  `title` VARCHAR(64),
  `start_date` DATETIME,
  `is_active` VARCHAR(64),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `users_chats` (
  `user_id` INT,
  `chat_id` INT,
  PRIMARY KEY (`user_id`, `chat_id`)
) CHARACTER SET latin1;

CREATE TABLE `messages` (
  `id` INT,
  `content` VARCHAR(64),
  `sent_on` DATETIME,
  `chat_id` INT,
  `user_id` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_568276_01_Database_Design') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_568276_01_Database_Design') AS relationships;
DROP DATABASE `db_568276_01_Database_Design`;
UNINSTALL EXTENSION vsql_schematron;
