INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 580093_1371487156
--echo #
CREATE DATABASE `db_580093_1371487156`;
USE `db_580093_1371487156`;

CREATE TABLE `apps_feedback_recipient_translation` (
  `rcp_id` INT,
  `lang_id` INT,
  `rcp_name` VARCHAR(64),
  PRIMARY KEY (`rcp_id`, `lang_id`)
) CHARACTER SET latin1;

CREATE TABLE `apps_feed_tags` (
  `tf_id` INT,
  `tag_id` INT,
  PRIMARY KEY (`tf_id`, `tag_id`)
) CHARACTER SET latin1;

CREATE TABLE `apps_feed_translation` (
  `tf_id` INT,
  `lang_id` INT,
  `tf_name` VARCHAR(64),
  `tf_annotation_rtf` VARCHAR(64),
  `tf_text_rtf` VARCHAR(64),
  PRIMARY KEY (`tf_id`, `lang_id`)
) CHARACTER SET latin1;

CREATE TABLE `apps_feed_uploads` (
  `tf_id` INT,
  `upl_id` INT,
  `upl_order_num` INT,
  PRIMARY KEY (`tf_id`, `upl_id`)
) CHARACTER SET latin1;

CREATE TABLE `apps_news_tags` (
  `news_id` INT,
  `tag_id` INT,
  PRIMARY KEY (`news_id`, `tag_id`)
) CHARACTER SET latin1;

CREATE TABLE `apps_news_translation` (
  `news_id` INT,
  `lang_id` INT,
  `news_title` VARCHAR(64),
  `news_announce_rtf` VARCHAR(64),
  `news_text_rtf` VARCHAR(64),
  PRIMARY KEY (`news_id`, `lang_id`)
) CHARACTER SET latin1;

CREATE TABLE `apps_news_uploads` (
  `news_id` INT,
  `upl_id` INT,
  `upl_order_num` INT,
  PRIMARY KEY (`news_id`, `upl_id`, `upl_order_num`)
) CHARACTER SET latin1;

CREATE TABLE `apps_vote_question_translation` (
  `vote_question_id` INT,
  `lang_id` INT,
  `vote_question_title` VARCHAR(64),
  PRIMARY KEY (`vote_question_id`, `lang_id`)
) CHARACTER SET latin1;

CREATE TABLE `apps_vote_translation` (
  `vote_id` INT,
  `lang_id` INT,
  `vote_name` VARCHAR(64),
  PRIMARY KEY (`vote_id`, `lang_id`)
) CHARACTER SET latin1;

CREATE TABLE `form_5_field_5_multi` (
  `pk_id` INT,
  `fk_id` INT,
  PRIMARY KEY (`pk_id`, `fk_id`)
) CHARACTER SET latin1;

CREATE TABLE `form_5_field_5_multi_values_translation` (
  `fk_id` INT,
  `lang_id` INT,
  `fk_name` VARCHAR(64),
  PRIMARY KEY (`fk_id`, `lang_id`)
) CHARACTER SET latin1;

CREATE TABLE `form_5_field_6_translation` (
  `fk_id` INT,
  `lang_id` INT,
  `fk_name` VARCHAR(64),
  PRIMARY KEY (`fk_id`, `lang_id`)
) CHARACTER SET latin1;

CREATE TABLE `frm_forms_translation` (
  `form_id` INT,
  `lang_id` INT,
  `form_name` VARCHAR(64),
  `form_annotation_rtf` VARCHAR(64),
  `form_post_annotation_rtf` VARCHAR(64),
  PRIMARY KEY (`form_id`, `lang_id`)
) CHARACTER SET latin1;

CREATE TABLE `share_access_level` (
  `smap_id` INT,
  `group_id` INT,
  `right_id` INT,
  PRIMARY KEY (`smap_id`, `group_id`, `right_id`)
) CHARACTER SET latin1;

CREATE TABLE `share_domain2site` (
  `domain_id` INT,
  `site_id` INT,
  PRIMARY KEY (`domain_id`, `site_id`)
) CHARACTER SET latin1;

CREATE TABLE `share_lang_tags_translation` (
  `ltag_id` INT,
  `lang_id` INT,
  `ltag_value_rtf` VARCHAR(64),
  PRIMARY KEY (`ltag_id`, `lang_id`)
) CHARACTER SET latin1;

CREATE TABLE `share_sitemap_tags` (
  `smap_id` INT,
  `tag_id` INT,
  PRIMARY KEY (`smap_id`, `tag_id`)
) CHARACTER SET latin1;

CREATE TABLE `share_sitemap_translation` (
  `smap_id` INT,
  `lang_id` INT,
  `smap_name` VARCHAR(64),
  `smap_description_rtf` VARCHAR(64),
  `smap_html_title` VARCHAR(64),
  `smap_meta_keywords` VARCHAR(64),
  `smap_meta_description` VARCHAR(64),
  `smap_is_disabled` INT,
  PRIMARY KEY (`lang_id`, `smap_id`)
) CHARACTER SET latin1;

CREATE TABLE `share_sitemap_uploads` (
  `smap_id` INT,
  `upl_id` INT,
  `upl_order_num` INT,
  PRIMARY KEY (`smap_id`, `upl_id`, `upl_order_num`)
) CHARACTER SET latin1;

CREATE TABLE `share_sites_tags` (
  `site_id` INT,
  `tag_id` INT,
  PRIMARY KEY (`site_id`, `tag_id`)
) CHARACTER SET latin1;

CREATE TABLE `share_sites_translation` (
  `site_id` INT,
  `lang_id` INT,
  `site_name` VARCHAR(64),
  `site_meta_keywords` VARCHAR(64),
  `site_meta_description` VARCHAR(64),
  PRIMARY KEY (`site_id`, `lang_id`)
) CHARACTER SET latin1;

CREATE TABLE `share_uploads_tags` (
  `upl_id` INT,
  `tag_id` INT,
  PRIMARY KEY (`upl_id`, `tag_id`)
) CHARACTER SET latin1;

CREATE TABLE `user_fb_storage` (
  `var_name` VARCHAR(64),
  `var_value` VARCHAR(64),
  PRIMARY KEY (`var_name`)
) CHARACTER SET latin1;

CREATE TABLE `user_users_ban` (
  `u_id` INT,
  `ban_date` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `user_user_groups` (
  `u_id` INT,
  `group_id` INT,
  PRIMARY KEY (`u_id`, `group_id`)
) CHARACTER SET latin1;

CREATE TABLE `share_sitemap_comment` (
  `comment_id` INT,
  `comment_parent_id` INT,
  `target_id` INT,
  `u_id` INT,
  `comment_created` DATETIME,
  `comment_name` VARCHAR(64),
  `comment_approved` INT,
  `comment_nick` VARCHAR(64),
  PRIMARY KEY (`comment_id`)
) CHARACTER SET latin1;

CREATE TABLE `apps_news_comment` (
  `comment_id` INT,
  `comment_parent_id` INT,
  `target_id` INT,
  `u_id` INT,
  `comment_created` DATETIME,
  `comment_name` VARCHAR(64),
  `comment_approved` INT,
  `comment_nick` VARCHAR(64),
  PRIMARY KEY (`comment_id`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_580093_1371487156') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_580093_1371487156') AS relationships;
DROP DATABASE `db_580093_1371487156`;
UNINSTALL EXTENSION vsql_schematron;
