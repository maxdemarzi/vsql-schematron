INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 535691_V3.2__CreateTpsAvspiller
--echo #
CREATE DATABASE `db_535691_V3_2__CreateTpsAvspiller`;
USE `db_535691_V3_2__CreateTpsAvspiller`;
CREATE TABLE `T_TPS_AVSPILLER` (
  `BESTILLING_ID` DECIMAL(10,2) NOT NULL,
  `PERIODE_FRA` DATE,
  `PERIODE_TIL` DATE,
  `KILDE_KOE` VARCHAR(50),
  `UTLOEP_KOE` VARCHAR(50),
  `FORMAT` VARCHAR(50),
  `REQUEST` VARCHAR(50),
  `ANTALL` DECIMAL(10,2),
  `TIDSPUNKT` DATE,
  `FERDIG` VARCHAR(50),
  `AVBRUTT` VARCHAR(50),
  `FEIL` VARCHAR(50),
  PRIMARY KEY (`BESTILLING_ID`)
) CHARACTER SET latin1;
CREATE TABLE `T_TPS_AVSPILLER_PROGRESS` (
  `PROGRESS_ID` DECIMAL(10,2) NOT NULL,
  `BESTILLING_ID` DECIMAL(10,2),
  `INDEKS_NR` DECIMAL(10,2),
  `MELDING_NR` DECIMAL(10,2),
  `TIDSPUNKT` DATE,
  `SEND_STATUS` VARCHAR(50),
  PRIMARY KEY (`PROGRESS_ID`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_535691_V3_2__CreateTpsAvspiller') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_535691_V3_2__CreateTpsAvspiller') AS relationships;
DROP DATABASE `db_535691_V3_2__CreateTpsAvspiller`;
UNINSTALL EXTENSION vsql_schematron;
