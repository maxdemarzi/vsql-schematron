INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 508723_musicdb
--echo #
CREATE DATABASE `db_508723_musicdb`;
USE `db_508723_musicdb`;

CREATE TABLE `login` (
  `Fname` VARCHAR(64),
  `Lname` VARCHAR(64),
  `email` VARCHAR(64),
  `password` VARCHAR(64),
  `DOB` DATETIME,
  `country` VARCHAR(64),
  `gender` VARCHAR(64),
  `userType` VARCHAR(64),
  `subscriptionPeriod` INT,
  `subscriptionCost` INT
) CHARACTER SET latin1;

CREATE TABLE `movies` (
  `movieName` VARCHAR(64),
  `movieID` INT,
  `director` VARCHAR(64),
  `actor` VARCHAR(64),
  `actress` VARCHAR(64),
  `language` VARCHAR(64),
  PRIMARY KEY (`movieID`)
) CHARACTER SET latin1;

CREATE TABLE `playlist` (
  `playlistName` VARCHAR(64),
  `playlistID` INT,
  `songID` INT
) CHARACTER SET latin1;

CREATE TABLE `singers` (
  `singerName` VARCHAR(64),
  `singerID` INT,
  `country` VARCHAR(64),
  `DOB` DATETIME,
  `Gender` VARCHAR(64),
  PRIMARY KEY (`singerID`)
) CHARACTER SET latin1;

CREATE TABLE `songs` (
  `songID` INT,
  `songName` VARCHAR(64),
  `singerID` INT,
  `movieID` INT,
  `lyricist` VARCHAR(64),
  `musicProducer` VARCHAR(64),
  `musicDistributer` VARCHAR(64),
  `language` VARCHAR(64),
  `genre` VARCHAR(64)
) CHARACTER SET latin1;

CREATE TABLE `trending` (
  `slno` INT,
  `songID` INT
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_508723_musicdb') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_508723_musicdb') AS relationships;
DROP DATABASE `db_508723_musicdb`;
UNINSTALL EXTENSION vsql_schematron;
