INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 563164_employee2_db
--echo #
CREATE DATABASE `db_563164_employee2_db`;
USE `db_563164_employee2_db`;
CREATE TABLE `roles` (
  `id` INT NOT NULL,
  `title` VARCHAR(50),
  `salary` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `department` (
  `id` INT NOT NULL,
  `department` VARCHAR(50),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `employee` (
  `id` INT NOT NULL,
  `first_name` VARCHAR(50),
  `last_name` VARCHAR(50),
  `roles_id` INT,
  `department_id` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_563164_employee2_db') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_563164_employee2_db') AS relationships;
DROP DATABASE `db_563164_employee2_db`;
UNINSTALL EXTENSION vsql_schematron;
