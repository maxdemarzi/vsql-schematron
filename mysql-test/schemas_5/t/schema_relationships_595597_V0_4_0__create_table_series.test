INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 595597_V0.4.0__create_table_series
--echo #
CREATE DATABASE `db_595597_V0_4_0__create_table_series`;
USE `db_595597_V0_4_0__create_table_series`;
CREATE TABLE `nama.series` (
  `id_series` VARCHAR(50) NOT NULL,
  `title` VARCHAR(50),
  `description` TEXT,
  `start_year` INT,
  `end_year` INT,
  `rating` VARCHAR(50),
  `modified` TIMESTAMP NULL,
  `previous_series_id` VARCHAR(50),
  PRIMARY KEY (`id_series`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_595597_V0_4_0__create_table_series') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_595597_V0_4_0__create_table_series') AS relationships;
DROP DATABASE `db_595597_V0_4_0__create_table_series`;
UNINSTALL EXTENSION vsql_schematron;
