INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 589451_gdo_database
--echo #
CREATE DATABASE `db_589451_gdo_database`;
USE `db_589451_gdo_database`;

CREATE TABLE `applicant` (
  `ApplicantId` INT,
  `RecordId` VARCHAR(64),
  `TimeSubmitted` DATETIME,
  `FirstName` VARCHAR(64),
  `LastName` VARCHAR(64),
  `Address` VARCHAR(64),
  `City` VARCHAR(64),
  `State` VARCHAR(64),
  `ZipCode` INT,
  `PhoneNumber` VARCHAR(64),
  `DateOfBirth` DATETIME,
  `Ethnicity` VARCHAR(64),
  `Allergies` VARCHAR(64),
  `Medications` VARCHAR(64),
  `ParentsWithBachelors` VARCHAR(64),
  `MilitaryRelatives` INT,
  `RisingGradeLevel` VARCHAR(64),
  `AttendingSchool` VARCHAR(64),
  `CollegeOfInterest` VARCHAR(64),
  `ShirtSize` VARCHAR(64),
  `WaiverStatus` VARCHAR(64),
  `ApplicationStatus` VARCHAR(64),
  PRIMARY KEY (`ApplicantId`)
) CHARACTER SET latin1;

CREATE TABLE `applicant_stem_interests` (
  `ApplicantId` INT,
  `DisciplineId` INT,
  PRIMARY KEY (`ApplicantId`, `DisciplineId`)
) CHARACTER SET latin1;

CREATE TABLE `emergency_contact` (
  `EmergencyId` INT,
  `ApplicantId` INT,
  `Name` VARCHAR(64),
  `Relationship` VARCHAR(64),
  `Address` VARCHAR(64),
  `City` VARCHAR(64),
  `State` VARCHAR(64),
  `ZipCode` INT,
  `MobilePhone` INT,
  `HomePhone` INT,
  `WorkPhone` INT,
  `InitialSignature` VARCHAR(64),
  PRIMARY KEY (`EmergencyId`)
) CHARACTER SET latin1;

CREATE TABLE `parent` (
  `ParentId` INT,
  `ApplicantId` INT,
  `FirstName` VARCHAR(64),
  `LastName` VARCHAR(64),
  `Email` VARCHAR(64),
  `Address` VARCHAR(64),
  `City` VARCHAR(64),
  `State` VARCHAR(64),
  `ZipCode` INT,
  `MobilePhone` INT,
  `WorkPhone` INT,
  `HomePhone` INT,
  `OtherParentFName` VARCHAR(64),
  `OtherParentLName` VARCHAR(64),
  PRIMARY KEY (`ParentId`)
) CHARACTER SET latin1;

CREATE TABLE `waivers` (
  `ApplicantId` INT,
  `WaiverType` INT,
  `WaiverStatus` VARCHAR(64),
  `WaiverLocation` VARCHAR(64)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_589451_gdo_database') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_589451_gdo_database') AS relationships;
DROP DATABASE `db_589451_gdo_database`;
UNINSTALL EXTENSION vsql_schematron;
