INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 602939_schema
#
CREATE DATABASE `db_602939_schema`;
USE `db_602939_schema`;
CREATE TABLE `users` (
`username` VARCHAR(50) NOT NULL,
`password` VARCHAR(50),
`enabled` TINYINT,
PRIMARY KEY (`username`)
) CHARACTER SET latin1;
CREATE TABLE `authorities` (
`id` INT NOT NULL,
`username` VARCHAR(50),
`authority` VARCHAR(50),
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_602939_schema') AS relationships;
relationships
Database: db_602939_schema

Table Connections:
------------------
[Implied]  authorities.username -> users.username

DROP DATABASE `db_602939_schema`;
UNINSTALL EXTENSION vsql_schematron;
