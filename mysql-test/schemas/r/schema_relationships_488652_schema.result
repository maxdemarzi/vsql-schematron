INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 488652_schema
#
CREATE DATABASE `db_488652_schema`;
USE `db_488652_schema`;
CREATE TABLE `role` (
`id` INT NOT NULL,
`title` VARCHAR(50),
`salary` DECIMAL(10,2),
`department_id` INT,
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `employee` (
`id` INT NOT NULL,
`first_name` VARCHAR(50),
`last_name` VARCHAR(50),
`role_id` INT,
`manager_id` INT,
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_488652_schema') AS relationships;
relationships
Database: db_488652_schema

Table Connections:
------------------
[Implied]  employee.manager_id -> employee.id
[Implied]  employee.role_id -> role.id

DROP DATABASE `db_488652_schema`;
UNINSTALL EXTENSION vsql_schematron;
