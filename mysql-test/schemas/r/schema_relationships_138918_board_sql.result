INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 138918_board_sql
#
CREATE DATABASE `db_138918_board_sql`;
USE `db_138918_board_sql`;
CREATE TABLE `members` (
`userid` VARCHAR(50) NOT NULL,
`userpw` VARCHAR(50),
`info` VARCHAR(50),
PRIMARY KEY (`userid`)
) CHARACTER SET latin1;
CREATE TABLE `board` (
`idx` DECIMAL(10,2) NOT NULL,
`title` VARCHAR(50),
`cont` VARCHAR(50),
`userid` VARCHAR(50),
PRIMARY KEY (`idx`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_138918_board_sql') AS relationships;
relationships
Database: db_138918_board_sql

Table Connections:
------------------
[Implied]  board.userid -> members.userid

DROP DATABASE `db_138918_board_sql`;
UNINSTALL EXTENSION vsql_schematron;
