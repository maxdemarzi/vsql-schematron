INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 285019_db-schema
#
CREATE DATABASE `db_285019_db_schema`;
USE `db_285019_db_schema`;
CREATE TABLE `users` (
`username` VARCHAR(50) NOT NULL,
`password` TEXT,
PRIMARY KEY (`username`)
) CHARACTER SET latin1;
CREATE TABLE `wallets` (
`username` VARCHAR(50),
`pin` TEXT
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_285019_db_schema') AS relationships;
relationships
Database: db_285019_db_schema

Table Connections:
------------------
[Implied]  wallets.username -> users.username

DROP DATABASE `db_285019_db_schema`;
UNINSTALL EXTENSION vsql_schematron;
