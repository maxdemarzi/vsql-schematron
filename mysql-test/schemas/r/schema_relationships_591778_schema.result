INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 591778_schema
#
CREATE DATABASE `db_591778_schema`;
USE `db_591778_schema`;
CREATE TABLE `user` (
`id` INT NOT NULL,
`username` TEXT,
`password` TEXT,
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `payment` (
`id` INT NOT NULL,
`user_id` INT,
`amount` DECIMAL(10,2),
`description` TEXT,
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_591778_schema') AS relationships;
relationships
Database: db_591778_schema

Table Connections:
------------------
[Implied]  payment.user_id -> user.id

DROP DATABASE `db_591778_schema`;
UNINSTALL EXTENSION vsql_schematron;
