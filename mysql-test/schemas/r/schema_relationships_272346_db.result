INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 272346_db
#
CREATE DATABASE `db_272346_db`;
USE `db_272346_db`;
CREATE TABLE `produtos` (
`id` INT NOT NULL,
`descricao` VARCHAR(50),
`estoque_minimo` INT,
`estoque_maximo` INT,
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `compras` (
`id` INT NOT NULL,
`produto_id` INT,
`quantidade` INT,
`preco` DECIMAL(10,2),
`data` DATE,
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `estoques` (
`id` INT NOT NULL,
`produto_id` INT,
`quantidade` INT,
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `vendas` (
`id` INT NOT NULL,
`produto_id` INT,
`quantidade` INT,
`data` DATE,
`preco` DECIMAL(10,2),
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_272346_db') AS relationships;
relationships
Database: db_272346_db

Table Connections:
------------------
[Implied]  compras.produto_id -> produtos.id
[Implied]  estoques.produto_id -> produtos.id
[Implied]  vendas.produto_id -> produtos.id

DROP DATABASE `db_272346_db`;
UNINSTALL EXTENSION vsql_schematron;
