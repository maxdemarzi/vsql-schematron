INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 467888_karopapier
#
CREATE DATABASE `db_467888_karopapier`;
USE `db_467888_karopapier`;
CREATE TABLE `user` (
`id` INT,
`name` TEXT,
`passwordHash` TEXT,
`isAdmin` INT
) CHARACTER SET latin1;
CREATE TABLE `session` (
`id` INT,
`userId` INT,
`token` TEXT,
`lastAccess` INT
) CHARACTER SET latin1;
CREATE TABLE `map` (
`id` INT,
`publicIdentifier` TEXT,
`name` TEXT,
`isActive` INT,
`width` INT,
`height` INT
) CHARACTER SET latin1;
CREATE TABLE `mapEntry` (
`mapId` INT,
`userId` INT,
`sessionId` INT,
`ip` TEXT,
`x` INT,
`y` INT,
`contentId` INT
) CHARACTER SET latin1;
CREATE TABLE `content` (
`id` INT,
`publicIdentifier` TEXT,
`name` TEXT
) CHARACTER SET latin1;
CREATE TABLE `mapContent` (
`mapId` INT,
`contentId` INT,
`groupNumber` INT
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_467888_karopapier') AS relationships;
relationships
Database: db_467888_karopapier

Table Connections:
------------------
[Implied]  mapContent.contentId -> content.id
[Implied]  mapContent.mapId -> map.id
[Implied]  mapEntry.contentId -> content.id
[Implied]  mapEntry.mapId -> map.id
[Implied]  mapEntry.sessionId -> session.id
[Implied]  mapEntry.userId -> user.id
[Implied]  session.userId -> user.id

DROP DATABASE `db_467888_karopapier`;
UNINSTALL EXTENSION vsql_schematron;
