INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 068843_schema
#
CREATE DATABASE `db_068843_schema`;
USE `db_068843_schema`;
CREATE TABLE `routes` (
`id` INT NOT NULL,
`path` TEXT,
`model` TEXT,
`view` TEXT,
`templateFile` TEXT,
`controller` TEXT,
`action` TEXT,
`requiresAuth` INT,
`requiredRole` TEXT,
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `actionArgs` (
`id` INT NOT NULL,
`argument` TEXT,
`type` TEXT,
`routeID` INT,
`position` INT,
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_068843_schema') AS relationships;
relationships
Database: db_068843_schema

Table Connections:
------------------
[Implied]  actionArgs.routeID -> routes.id

DROP DATABASE `db_068843_schema`;
UNINSTALL EXTENSION vsql_schematron;
