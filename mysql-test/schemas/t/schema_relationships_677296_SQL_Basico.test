INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 677296_SQL_Basico
--echo #
CREATE DATABASE `db_677296_SQL_Basico`;
USE `db_677296_SQL_Basico`;
CREATE TABLE `books` (
  `book_id` VARCHAR(50) NOT NULL,
  `book_category_id` VARCHAR(50),
  `title` VARCHAR(50),
  `date_publish` DATE,
  `date_acquisition` DATE,
  `book_comments` VARCHAR(50),
  `price` DECIMAL(10,2),
  `author_id` VARCHAR(50),
  PRIMARY KEY (`book_id`)
) CHARACTER SET latin1;
CREATE TABLE `consumers` (
  `consumer_id` VARCHAR(50) NOT NULL,
  `first_name` VARCHAR(50),
  `last_name` VARCHAR(50),
  `gender` CHAR(50),
  `age` INT,
  `date_cadastro` TIMESTAMP NULL,
  `city` VARCHAR(50),
  `email` VARCHAR(50),
  PRIMARY KEY (`consumer_id`)
) CHARACTER SET latin1;
CREATE TABLE `orders` (
  `order_id` VARCHAR(50) NOT NULL,
  `consumer_id` VARCHAR(50),
  `order_date` TIMESTAMP NULL,
  `order_value` VARCHAR(50),
  PRIMARY KEY (`order_id`)
) CHARACTER SET latin1;
CREATE TABLE `book_category` (
  `book_category_id` VARCHAR(50) NOT NULL,
  `category_type` VARCHAR(50),
  `book_description` VARCHAR(50),
  PRIMARY KEY (`book_category_id`)
) CHARACTER SET latin1;
CREATE TABLE `authors` (
  `author_id` VARCHAR(50) NOT NULL,
  `first_name` VARCHAR(50),
  `last_name` VARCHAR(50),
  `gender` CHAR(50),
  `date_of_birth` DATE,
  `email` VARCHAR(50),
  PRIMARY KEY (`author_id`)
) CHARACTER SET latin1;
CREATE TABLE `order_items` (
  `item_id` VARCHAR(50) NOT NULL,
  `order_id` VARCHAR(50),
  `book_id` VARCHAR(50),
  `item_price` DECIMAL(10,2),
  `item_comment` VARCHAR(50),
  PRIMARY KEY (`item_id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_677296_SQL_Basico') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_677296_SQL_Basico') AS relationships;
DROP DATABASE `db_677296_SQL_Basico`;
UNINSTALL EXTENSION vsql_schematron;
