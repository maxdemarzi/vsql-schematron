INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 604078_database
--echo #
CREATE DATABASE `db_604078_database`;
USE `db_604078_database`;
CREATE TABLE `chefs` (
  `id` INT NOT NULL,
  `name` TEXT,
  `avatar_url` TEXT,
  `created_at` TIMESTAMP NULL,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `recipes` (
  `id` INT NOT NULL,
  `chef_id` INT,
  `image` TEXT,
  `title` TEXT,
  `ingredients` VARCHAR(50),
  `preparations` VARCHAR(50),
  `information` TEXT,
  `created_at` TIMESTAMP NULL,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_604078_database') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_604078_database') AS relationships;
DROP DATABASE `db_604078_database`;
UNINSTALL EXTENSION vsql_schematron;
