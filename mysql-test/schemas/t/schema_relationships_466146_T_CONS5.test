INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 466146_T_CONS5
--echo #
CREATE DATABASE `db_466146_T_CONS5`;
USE `db_466146_T_CONS5`;
CREATE TABLE `何辑` (
  `何辑ID` VARCHAR(50) NOT NULL,
  `何辑疙` VARCHAR(50),
  PRIMARY KEY (`何辑ID`)
) CHARACTER SET latin1;
CREATE TABLE `EMPLOYEE` (
  `EMPID` VARCHAR(50),
  `何辑ID` VARCHAR(50)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_466146_T_CONS5') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_466146_T_CONS5') AS relationships;
DROP DATABASE `db_466146_T_CONS5`;
UNINSTALL EXTENSION vsql_schematron;
