INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 397051_bridgedb_schema_upgrade_8.0.5_to_8.0.6
--echo #
CREATE DATABASE `db_397051_bridgedb_schema_upgrade_8_0_5_to_8_0_6`;
USE `db_397051_bridgedb_schema_upgrade_8_0_5_to_8_0_6`;
CREATE TABLE `bridgedb.Trader` (
  `Id` INT NOT NULL,
  `Tenant` INT,
  `Marketer` INT,
  `OpportunityType` VARCHAR(50),
  `updatedChannel` INT,
  `updatedVenture` INT,
  `updatedIE` DECIMAL(10,2),
  `createdDateTime` DATETIME,
  `notes` VARCHAR(50),
  `createdIE` DECIMAL(10,2),
  `updatedUser` INT,
  `inActiveReason` VARCHAR(50),
  `appCreatedDateTime` DATETIME,
  `inActiveDateTime` DATETIME,
  `createdTenant` INT,
  `createdChannel` INT,
  `updatedTenant` INT,
  `updatedDateTime` DATETIME,
  `createdUser` INT,
  `appUpdatedDateTime` DATETIME,
  `aliasId` VARCHAR(50),
  `iconpic` VARCHAR(50),
  `isActive` TINYINT,
  `createdVenture` INT,
  PRIMARY KEY (`Id`)
) CHARACTER SET latin1;
CREATE TABLE `bridgedb.TraderProduct` (
  `Id` INT NOT NULL,
  `Trader` INT,
  `Tenant` INT,
  `Product` INT,
  `Quantity` INT,
  `Frequency` INT,
  `Price` INT,
  `PriceUOM` INT,
  `updatedChannel` INT,
  `updatedVenture` INT,
  `updatedIE` DECIMAL(10,2),
  `createdDateTime` DATETIME,
  `notes` VARCHAR(50),
  `createdIE` DECIMAL(10,2),
  `updatedUser` INT,
  `inActiveReason` VARCHAR(50),
  `appCreatedDateTime` DATETIME,
  `inActiveDateTime` DATETIME,
  `createdTenant` INT,
  `createdChannel` INT,
  `updatedTenant` INT,
  `updatedDateTime` DATETIME,
  `createdUser` INT,
  `appUpdatedDateTime` DATETIME,
  `aliasId` VARCHAR(50),
  `iconpic` VARCHAR(50),
  `isActive` TINYINT,
  `createdVenture` INT,
  PRIMARY KEY (`Id`)
) CHARACTER SET latin1;
CREATE TABLE `bridgedb.CustomerLead` (
  `Customer` INT NOT NULL,
  `Lead` INT NOT NULL,
  PRIMARY KEY (`Customer`, `Lead`)
) CHARACTER SET latin1;
CREATE TABLE `bridgedb.RefDataConfigScopeMaster` (
  `Id` INT NOT NULL,
  `Code` VARCHAR(50),
  `Name` VARCHAR(50),
  `Description` VARCHAR(50),
  `DataType` VARCHAR(50),
  `Reference` TEXT,
  `Priority` INT,
  `updatedChannel` INT,
  `updatedVenture` INT,
  `updatedIE` DECIMAL(10,2),
  `createdDateTime` DATETIME,
  `notes` VARCHAR(50),
  `createdIE` DECIMAL(10,2),
  `updatedUser` INT,
  `inActiveReason` VARCHAR(50),
  `appCreatedDateTime` DATETIME,
  `inActiveDateTime` DATETIME,
  `createdTenant` INT,
  `createdChannel` INT,
  `updatedTenant` INT,
  `updatedDateTime` DATETIME,
  `createdUser` INT,
  `appUpdatedDateTime` DATETIME,
  `aliasId` VARCHAR(50),
  `iconpic` VARCHAR(50),
  `isActive` TINYINT,
  `createdVenture` INT,
  PRIMARY KEY (`Id`)
) CHARACTER SET latin1;
CREATE TABLE `bridgedb.RefDataConfig` (
  `Id` INT NOT NULL,
  `ScopeCode` VARCHAR(50),
  `ScopeValue` TEXT,
  `ConfigKey` VARCHAR(50),
  `ConfigValue` TEXT,
  `updatedChannel` INT,
  `updatedVenture` INT,
  `updatedIE` DECIMAL(10,2),
  `createdDateTime` DATETIME,
  `notes` VARCHAR(50),
  `createdIE` DECIMAL(10,2),
  `updatedUser` INT,
  `inActiveReason` VARCHAR(50),
  `appCreatedDateTime` DATETIME,
  `inActiveDateTime` DATETIME,
  `createdTenant` INT,
  `createdChannel` INT,
  `updatedTenant` INT,
  `updatedDateTime` DATETIME,
  `createdUser` INT,
  `appUpdatedDateTime` DATETIME,
  `aliasId` VARCHAR(50),
  `iconpic` VARCHAR(50),
  `isActive` TINYINT,
  `createdVenture` INT,
  PRIMARY KEY (`Id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_397051_bridgedb_schema_upgrade_8_0_5_to_8_0_6') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_397051_bridgedb_schema_upgrade_8_0_5_to_8_0_6') AS relationships;
DROP DATABASE `db_397051_bridgedb_schema_upgrade_8_0_5_to_8_0_6`;
UNINSTALL EXTENSION vsql_schematron;
