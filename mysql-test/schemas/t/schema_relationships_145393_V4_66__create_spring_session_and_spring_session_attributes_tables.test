INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 145393_V4.66__create_spring_session_and_spring_session_attributes_tables
--echo #
CREATE DATABASE `db_145393_V4_66__create_spring_session_and_spring_session_attrib`;
USE `db_145393_V4_66__create_spring_session_and_spring_session_attrib`;
CREATE TABLE `ptcore.SPRING_SESSION` (
  `PRIMARY_ID` VARCHAR(50) NOT NULL,
  `SESSION_ID` CHAR(50),
  `CREATION_TIME` BIGINT,
  `LAST_ACCESS_TIME` BIGINT,
  `MAX_INACTIVE_INTERVAL` INT,
  `EXPIRY_TIME` BIGINT,
  `PRINCIPAL_NAME` VARCHAR(50),
  PRIMARY KEY (`PRIMARY_ID`)
) CHARACTER SET latin1;
CREATE TABLE `ptcore.SPRING_SESSION_ATTRIBUTES` (
  `SESSION_PRIMARY_ID` VARCHAR(50) NOT NULL,
  `ATTRIBUTE_NAME` VARCHAR(50) NOT NULL,
  `ATTRIBUTE_BYTES` VARCHAR(50),
  PRIMARY KEY (`SESSION_PRIMARY_ID`, `ATTRIBUTE_NAME`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_145393_V4_66__create_spring_session_and_spring_session_attrib') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_145393_V4_66__create_spring_session_and_spring_session_attrib') AS relationships;
DROP DATABASE `db_145393_V4_66__create_spring_session_and_spring_session_attrib`;
UNINSTALL EXTENSION vsql_schematron;
