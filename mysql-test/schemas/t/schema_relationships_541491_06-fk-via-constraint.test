INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 541491_06-fk-via-constraint
--echo #
CREATE DATABASE `db_541491_06_fk_via_constraint`;
USE `db_541491_06_fk_via_constraint`;
CREATE TABLE `lusers` (
  `id` INT NOT NULL,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `foo` (
  `user_id` INT
) CHARACTER SET latin1;
CREATE TABLE `bar` (
  `user_id` INT
) CHARACTER SET latin1;
CREATE TABLE `qux` (
  `user_id` INT
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_541491_06_fk_via_constraint') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_541491_06_fk_via_constraint') AS relationships;
DROP DATABASE `db_541491_06_fk_via_constraint`;
UNINSTALL EXTENSION vsql_schematron;
