INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 526192_tables_referencing_each_other
--echo #
CREATE DATABASE `db_526192_tables_referencing_each_other`;
USE `db_526192_tables_referencing_each_other`;
CREATE TABLE `test_table_1` (
  `id` INT NOT NULL,
  `table_2_id` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `test_table_2` (
  `id` INT NOT NULL,
  `table_1_id` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_526192_tables_referencing_each_other') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_526192_tables_referencing_each_other') AS relationships;
DROP DATABASE `db_526192_tables_referencing_each_other`;
UNINSTALL EXTENSION vsql_schematron;
