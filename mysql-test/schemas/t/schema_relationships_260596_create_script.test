INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 260596_create_script
--echo #
CREATE DATABASE `db_260596_create_script`;
USE `db_260596_create_script`;
CREATE TABLE `area_desempenio` (
  `id_area` INT NOT NULL,
  `nombre_area` VARCHAR(50),
  PRIMARY KEY (`id_area`)
) CHARACTER SET latin1;
CREATE TABLE `grado_academico` (
  `id_grado` INT NOT NULL,
  `nombre_grado` VARCHAR(50),
  PRIMARY KEY (`id_grado`)
) CHARACTER SET latin1;
CREATE TABLE `persona` (
  `id_persona` INT NOT NULL,
  `nombres_persona` VARCHAR(50),
  `apellidos_persona` VARCHAR(50),
  `telefono_persona` VARCHAR(50),
  `direccion_persona` VARCHAR(50),
  `fecha_nacimiento` DATE,
  `usuario_persona` VARCHAR(50),
  `contrasenia_persona` VARCHAR(50),
  PRIMARY KEY (`id_persona`)
) CHARACTER SET latin1;
CREATE TABLE `registro_grado` (
  `id_persona` INT NOT NULL,
  `id_grado` INT NOT NULL,
  `id_area` INT NOT NULL,
  `fecha_registro` DATE,
  PRIMARY KEY (`id_persona`, `id_grado`, `id_area`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_260596_create_script') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_260596_create_script') AS relationships;
DROP DATABASE `db_260596_create_script`;
UNINSTALL EXTENSION vsql_schematron;
