INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 445128_20200402165728-create-regions-table-up
--echo #
CREATE DATABASE `db_445128_20200402165728_create_regions_table_up`;
USE `db_445128_20200402165728_create_regions_table_up`;
CREATE TABLE `regions` (
  `id` VARCHAR(50) NOT NULL,
  `name` TEXT,
  `parent_id` VARCHAR(50),
  `created_at` DATETIME,
  `updated_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_445128_20200402165728_create_regions_table_up') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_445128_20200402165728_create_regions_table_up') AS relationships;
DROP DATABASE `db_445128_20200402165728_create_regions_table_up`;
UNINSTALL EXTENSION vsql_schematron;
