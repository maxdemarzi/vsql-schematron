INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 068231_20200630141158_update-model-version-foreign-keys.up
--echo #
CREATE DATABASE `db_068231_20200630141158_update_model_version_foreign_keys_up`;
USE `db_068231_20200630141158_update_model_version_foreign_keys_up`;
CREATE TABLE `public.models` (
  `name` VARCHAR(50) NOT NULL,
  `description` CHAR(50),
  `creation_time` TIMESTAMP NULL,
  `last_updated_time` TIMESTAMP NULL,
  `metadata` VARCHAR(50),
  PRIMARY KEY (`name`)
) CHARACTER SET latin1;
CREATE TABLE `public.model_versions` (
  `version` INT NOT NULL,
  `model_name` VARCHAR(50) NOT NULL,
  `checkpoint_uuid` VARCHAR(50),
  `creation_time` TIMESTAMP NULL,
  `last_updated_time` TIMESTAMP NULL,
  `metadata` VARCHAR(50),
  PRIMARY KEY (`model_name`, `version`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_068231_20200630141158_update_model_version_foreign_keys_up') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_068231_20200630141158_update_model_version_foreign_keys_up') AS relationships;
DROP DATABASE `db_068231_20200630141158_update_model_version_foreign_keys_up`;
UNINSTALL EXTENSION vsql_schematron;
