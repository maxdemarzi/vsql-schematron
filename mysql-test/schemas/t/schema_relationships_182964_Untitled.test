INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 182964_Untitled
--echo #
CREATE DATABASE `db_182964_Untitled`;
USE `db_182964_Untitled`;
CREATE TABLE `chefs` (
  `id` INT NOT NULL,
  `name` TEXT,
  `file_id` INT,
  `created_at` TIMESTAMP NULL,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `recipes` (
  `id` INT NOT NULL,
  `chef_id` INT,
  `title` TEXT,
  `information` TEXT,
  `ingredients` VARCHAR(50),
  `preparation` VARCHAR(50),
  `created_at` TIMESTAMP NULL,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `files` (
  `id` INT NOT NULL,
  `name` TEXT,
  `path` TEXT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `recipes_files` (
  `id` INT NOT NULL,
  `recipe_id` INT,
  `file_id` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_182964_Untitled') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_182964_Untitled') AS relationships;
DROP DATABASE `db_182964_Untitled`;
UNINSTALL EXTENSION vsql_schematron;
