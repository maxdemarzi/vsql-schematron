INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 168785_portfolio-schema
--echo #
CREATE DATABASE `db_168785_portfolio_schema`;
USE `db_168785_portfolio_schema`;
CREATE TABLE `Portfolio` (
  `owner` VARCHAR(50) NOT NULL,
  `total` DOUBLE,
  `loyalty` VARCHAR(50),
  `balance` DOUBLE,
  `commissions` DOUBLE,
  `free` INT,
  `sentiment` VARCHAR(50),
  PRIMARY KEY (`owner`)
) CHARACTER SET latin1;
CREATE TABLE `Stock` (
  `owner` VARCHAR(50) NOT NULL,
  `symbol` VARCHAR(50) NOT NULL,
  `shares` INT,
  `price` DOUBLE,
  `total` DOUBLE,
  `dateQuoted` DATE,
  `commission` DOUBLE,
  PRIMARY KEY (`owner`, `symbol`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_168785_portfolio_schema') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_168785_portfolio_schema') AS relationships;
DROP DATABASE `db_168785_portfolio_schema`;
UNINSTALL EXTENSION vsql_schematron;
