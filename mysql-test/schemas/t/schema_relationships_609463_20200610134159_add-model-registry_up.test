INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 609463_20200610134159_add-model-registry.up
--echo #
CREATE DATABASE `db_609463_20200610134159_add_model_registry_up`;
USE `db_609463_20200610134159_add_model_registry_up`;
CREATE TABLE `public.models` (
  `id` INT NOT NULL,
  `name` CHAR(50),
  `description` CHAR(50),
  `metadata` VARCHAR(50),
  `creation_time` TIMESTAMP NULL,
  `last_updated_time` TIMESTAMP NULL,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `public.model_versions` (
  `id` INT NOT NULL,
  `version` INT,
  `model_id` INT,
  `checkpoint_id` INT,
  `metadata` VARCHAR(50),
  `creation_time` TIMESTAMP NULL,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_609463_20200610134159_add_model_registry_up') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_609463_20200610134159_add_model_registry_up') AS relationships;
DROP DATABASE `db_609463_20200610134159_add_model_registry_up`;
UNINSTALL EXTENSION vsql_schematron;
