INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 220451_tables
--echo #
CREATE DATABASE `db_220451_tables`;
USE `db_220451_tables`;
CREATE TABLE `SystemUsers` (
  `Id` INT NOT NULL,
  `Login` VARCHAR(50),
  `Password` VARCHAR(50),
  `FullName` VARCHAR(50),
  PRIMARY KEY (`Id`)
) CHARACTER SET latin1;
CREATE TABLE `Sessions` (
  `Id` INT NOT NULL,
  `UserId` INT,
  `Token` VARCHAR(50),
  `Data` TEXT,
  PRIMARY KEY (`Id`)
) CHARACTER SET latin1;
CREATE TABLE `Posts` (
  `Id` INT NOT NULL,
  `UserId` INT,
  `Data` TEXT,
  `Subject` VARCHAR(50),
  PRIMARY KEY (`Id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_220451_tables') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_220451_tables') AS relationships;
DROP DATABASE `db_220451_tables`;
UNINSTALL EXTENSION vsql_schematron;
