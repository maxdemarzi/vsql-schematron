INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 167458_main
--echo #
CREATE DATABASE `db_167458_main`;
USE `db_167458_main`;
CREATE TABLE `users` (
  `id` INT NOT NULL,
  `username` TEXT,
  `password` TEXT,
  `salt` TEXT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `canvasses` (
  `id` INT NOT NULL,
  `title` TEXT,
  `owner_id` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `notes` (
  `id` INT NOT NULL,
  `title` VARCHAR(50),
  `owner_id` INT,
  `body` TEXT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `placements` (
  `id` INT NOT NULL,
  `notes_id` INT,
  `canvas_id` INT,
  `coordinates` VARCHAR(50),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_167458_main') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_167458_main') AS relationships;
DROP DATABASE `db_167458_main`;
UNINSTALL EXTENSION vsql_schematron;
