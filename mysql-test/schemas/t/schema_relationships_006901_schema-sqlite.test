INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 006901_schema-sqlite
--echo #
CREATE DATABASE `db_006901_schema_sqlite`;
USE `db_006901_schema_sqlite`;
CREATE TABLE `SPRING_SESSION` (
  `PRIMARY_ID` VARCHAR(50) NOT NULL,
  `SESSION_ID` CHAR(50),
  `CREATION_TIME` INT,
  `LAST_ACCESS_TIME` INT,
  `MAX_INACTIVE_INTERVAL` INT,
  `EXPIRY_TIME` INT,
  `PRINCIPAL_NAME` VARCHAR(50),
  PRIMARY KEY (`PRIMARY_ID`)
) CHARACTER SET latin1;
CREATE TABLE `SPRING_SESSION_ATTRIBUTES` (
  `SESSION_PRIMARY_ID` VARCHAR(50) NOT NULL,
  `ATTRIBUTE_NAME` VARCHAR(50) NOT NULL,
  `ATTRIBUTE_BYTES` BLOB,
  PRIMARY KEY (`SESSION_PRIMARY_ID`, `ATTRIBUTE_NAME`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_006901_schema_sqlite') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_006901_schema_sqlite') AS relationships;
DROP DATABASE `db_006901_schema_sqlite`;
UNINSTALL EXTENSION vsql_schematron;
