INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 211658_HR_elt
--echo #
CREATE DATABASE `db_211658_HR_elt`;
USE `db_211658_HR_elt`;
CREATE TABLE `REGIONS` (
  `REGION_ID` VARCHAR(50) NOT NULL,
  `REGION_NAME` VARCHAR(50),
  PRIMARY KEY (`REGION_ID`)
) CHARACTER SET latin1;
CREATE TABLE `COUNTRIES` (
  `COUNTRY_ID` VARCHAR(50) NOT NULL,
  `COUNTRY_NAME` VARCHAR(50),
  `REGION_ID` VARCHAR(50),
  PRIMARY KEY (`COUNTRY_ID`)
) CHARACTER SET latin1;
CREATE TABLE `LOCATIONS` (
  `LOCATION_ID` VARCHAR(50) NOT NULL,
  `STREET_ADDRESS` VARCHAR(50),
  `POSTAL_CODE` VARCHAR(50),
  `CITY` VARCHAR(50),
  `STATE_PROVINCE` VARCHAR(50),
  `COUNTRY_ID` VARCHAR(50),
  PRIMARY KEY (`LOCATION_ID`)
) CHARACTER SET latin1;
CREATE TABLE `DEPARTMENTS` (
  `DEPARTMENT_ID` VARCHAR(50) NOT NULL,
  `DEPARTMENT_NAME` VARCHAR(50),
  `LOCATION_ID` VARCHAR(50),
  PRIMARY KEY (`DEPARTMENT_ID`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_211658_HR_elt') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_211658_HR_elt') AS relationships;
DROP DATABASE `db_211658_HR_elt`;
UNINSTALL EXTENSION vsql_schematron;
