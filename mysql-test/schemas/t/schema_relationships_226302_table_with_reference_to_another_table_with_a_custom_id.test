INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 226302_table_with_reference_to_another_table_with_a_custom_id
--echo #
CREATE DATABASE `db_226302_table_with_reference_to_another_table_with_a_custom_id`;
USE `db_226302_table_with_reference_to_another_table_with_a_custom_id`;
CREATE TABLE `test_table_1` (
  `id` INT NOT NULL,
  `table_2_id` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `test_table_2` (
  `custom_id` INT NOT NULL,
  PRIMARY KEY (`custom_id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_226302_table_with_reference_to_another_table_with_a_custom_id') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_226302_table_with_reference_to_another_table_with_a_custom_id') AS relationships;
DROP DATABASE `db_226302_table_with_reference_to_another_table_with_a_custom_id`;
UNINSTALL EXTENSION vsql_schematron;
