INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 693764_whatabook_init
--echo #
CREATE DATABASE `db_693764_whatabook_init`;
USE `db_693764_whatabook_init`;
CREATE TABLE `user` (
  `id` INT NOT NULL,
  `first_name` VARCHAR(50),
  `last_name` VARCHAR(50),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `wishlist` (
  `id` INT NOT NULL,
  `user_id` INT,
  `book_id` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `book` (
  `id` INT NOT NULL,
  `title` VARCHAR(50),
  `details` VARCHAR(50),
  `author` VARCHAR(50),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `store` (
  `id` INT NOT NULL,
  `locale` VARCHAR(50),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_693764_whatabook_init') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_693764_whatabook_init') AS relationships;
DROP DATABASE `db_693764_whatabook_init`;
UNINSTALL EXTENSION vsql_schematron;
