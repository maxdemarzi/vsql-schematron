INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 153789_V1.0.0__INITIAL_MODEL
--echo #
CREATE DATABASE `db_153789_V1_0_0__INITIAL_MODEL`;
USE `db_153789_V1_0_0__INITIAL_MODEL`;
CREATE TABLE `PRODUCTS` (
  `ID` DECIMAL(10,2) NOT NULL,
  `PRODUCT_ID` VARCHAR(50),
  `NAME` VARCHAR(50),
  `DESCRIPTION` VARCHAR(50),
  `STOCK` DECIMAL(10,2),
  `PRICE` DECIMAL(10,2),
  `CURRENCY` VARCHAR(50),
  `CREATED` TIMESTAMP NULL,
  `MODIFIED` TIMESTAMP NULL,
  PRIMARY KEY (`ID`)
) CHARACTER SET latin1;
CREATE TABLE `RESERVATIONS` (
  `ID` DECIMAL(10,2) NOT NULL,
  `RESERVATION_ID` VARCHAR(50),
  `PRODUCT_ID` DECIMAL(10,2),
  `ORDER_ID` VARCHAR(50),
  `QUANTITY` DECIMAL(10,2),
  `STATUS` VARCHAR(50),
  `CREATED` TIMESTAMP NULL,
  `MODIFIED` TIMESTAMP NULL,
  PRIMARY KEY (`ID`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_153789_V1_0_0__INITIAL_MODEL') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_153789_V1_0_0__INITIAL_MODEL') AS relationships;
DROP DATABASE `db_153789_V1_0_0__INITIAL_MODEL`;
UNINSTALL EXTENSION vsql_schematron;
