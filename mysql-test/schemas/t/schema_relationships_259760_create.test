INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 259760_create
--echo #
CREATE DATABASE `db_259760_create`;
USE `db_259760_create`;
CREATE TABLE `a02_title` (
  `id` VARCHAR(50) NOT NULL,
  `title` TEXT,
  `body` TEXT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `a02_title_to_tag` (
  `id` VARCHAR(50) NOT NULL,
  `title_id` VARCHAR(50),
  `tag_id` VARCHAR(50),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `a02_tags` (
  `id` VARCHAR(50) NOT NULL,
  `tag` TEXT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_259760_create') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_259760_create') AS relationships;
DROP DATABASE `db_259760_create`;
UNINSTALL EXTENSION vsql_schematron;
