INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 299865_V10_1__add_missing_folder_metadata_constraints
--echo #
CREATE DATABASE `db_299865_V10_1__add_missing_folder_metadata_constraints`;
USE `db_299865_V10_1__add_missing_folder_metadata_constraints`;
CREATE TABLE `folder_metadata` (
  `folder_id` INT NOT NULL,
  `name` TEXT,
  `path_lower` TEXT,
  `path_display` TEXT,
  `parent_folder_id` INT,
  `server_created_at` TEXT,
  `owner_id` INT,
  PRIMARY KEY (`folder_id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_299865_V10_1__add_missing_folder_metadata_constraints') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_299865_V10_1__add_missing_folder_metadata_constraints') AS relationships;
DROP DATABASE `db_299865_V10_1__add_missing_folder_metadata_constraints`;
UNINSTALL EXTENSION vsql_schematron;
