INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 274250_V20170227185638__egf_Fundsource_ddl
--echo #
CREATE DATABASE `db_274250_V20170227185638__egf_Fundsource_ddl`;
USE `db_274250_V20170227185638__egf_Fundsource_ddl`;
CREATE TABLE `egf_fundsource` (
  `id` BIGINT NOT NULL,
  `code` VARCHAR(50),
  `name` VARCHAR(50),
  `type` VARCHAR(50),
  `parentid` BIGINT,
  `llevel` DECIMAL(10,2),
  `active` BOOLEAN,
  `isParent` BOOLEAN,
  `createdby` BIGINT,
  `createddate` TIMESTAMP NULL,
  `lastmodifiedby` BIGINT,
  `lastmodifieddate` TIMESTAMP NULL,
  `version` BIGINT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_274250_V20170227185638__egf_Fundsource_ddl') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_274250_V20170227185638__egf_Fundsource_ddl') AS relationships;
DROP DATABASE `db_274250_V20170227185638__egf_Fundsource_ddl`;
UNINSTALL EXTENSION vsql_schematron;
