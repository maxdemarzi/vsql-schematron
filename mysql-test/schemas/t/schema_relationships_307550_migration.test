INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 307550_migration
--echo #
CREATE DATABASE `db_307550_migration`;
USE `db_307550_migration`;
CREATE TABLE `user` (
  `id` INT NOT NULL,
  `email` TEXT,
  `password` TEXT,
  `name` TEXT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `item` (
  `id` INT NOT NULL,
  `createdAt` TIMESTAMP NULL,
  `updatedAt` TIMESTAMP NULL,
  `name` TEXT,
  `type` TEXT,
  `userId` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_307550_migration') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_307550_migration') AS relationships;
DROP DATABASE `db_307550_migration`;
UNINSTALL EXTENSION vsql_schematron;
