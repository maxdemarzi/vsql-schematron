INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 453104_tables_referencing_each_other
--echo #
CREATE DATABASE `db_453104_tables_referencing_each_other`;
USE `db_453104_tables_referencing_each_other`;
CREATE TABLE `TestTable1` (
  `id` BIGINT NOT NULL,
  `table2Id` BIGINT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `TestTable2` (
  `id` BIGINT NOT NULL,
  `table1Id` BIGINT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_453104_tables_referencing_each_other') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_453104_tables_referencing_each_other') AS relationships;
DROP DATABASE `db_453104_tables_referencing_each_other`;
UNINSTALL EXTENSION vsql_schematron;
