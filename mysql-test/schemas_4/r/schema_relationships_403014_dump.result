INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 403014_dump
#
CREATE DATABASE `db_403014_dump`;
USE `db_403014_dump`;
CREATE TABLE `pop` (
`sock_id` VARCHAR(255),
`name` VARCHAR(255),
`description` VARCHAR(255),
`price` DECIMAL(10,2),
`count` INT,
`image_url_1` VARCHAR(255),
`image_url_2` VARCHAR(255)
) CHARACTER SET latin1;
CREATE TABLE `tag` (
`tag_id` INT,
`name` VARCHAR(255),
PRIMARY KEY (`tag_id`)
) CHARACTER SET latin1;
CREATE TABLE `pop_tag` (
`pop_id` VARCHAR(255),
`tag_id` INT
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_403014_dump') AS relationships;
relationships
Database: db_403014_dump

Table Connections:
------------------
[Implied]  pop_tag.tag_id -> tag.tag_id

DROP DATABASE `db_403014_dump`;
UNINSTALL EXTENSION vsql_schematron;
