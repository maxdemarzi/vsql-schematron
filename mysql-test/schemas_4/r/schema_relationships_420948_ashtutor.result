INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 420948_ashtutor
#
CREATE DATABASE `db_420948_ashtutor`;
USE `db_420948_ashtutor`;
CREATE TABLE `comments` (
`commentID` INT,
`author` INT,
`dateCreated` DATETIME,
`label` VARCHAR(64),
`vidID` INT
) CHARACTER SET latin1;
CREATE TABLE `users` (
`userID` INT,
`fName` VARCHAR(64),
`lName` VARCHAR(64),
`email` VARCHAR(64),
`pwd` VARCHAR(64),
`phone` VARCHAR(64),
`role` VARCHAR(64),
PRIMARY KEY (`userID`)
) CHARACTER SET latin1;
CREATE TABLE `video` (
`vidId` INT,
`author` INT,
`dateCreated` DATETIME,
`vidTopic` VARCHAR(64),
`numViews` INT,
`rating` DECIMAL(10,2),
`approved` VARCHAR(64)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_420948_ashtutor') AS relationships;
relationships
Database: db_420948_ashtutor

Table Connections:
------------------
(No relationships found)

DROP DATABASE `db_420948_ashtutor`;
UNINSTALL EXTENSION vsql_schematron;
