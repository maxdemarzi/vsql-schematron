INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 418343_initial_states
#
CREATE DATABASE `db_418343_initial_states`;
USE `db_418343_initial_states`;
CREATE TABLE `coneg.dim_cadastrados` (
`pesid` INT,
`nome` VARCHAR(64),
`email` VARCHAR(64),
`telefone` VARCHAR(64),
`notificado` BOOL,
PRIMARY KEY (`pesid`)
) CHARACTER SET latin1;
CREATE TABLE `coneg.dim_status` (
`id` INT,
`mascara` BOOL,
`cadastrado` INT,
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `coneg.fato_faces` (
`tid` INT,
`local` VARCHAR(64),
`ts` DATETIME,
`status` INT,
`pessoa` INT,
PRIMARY KEY (`tid`)
) CHARACTER SET latin1;
CREATE TABLE `coneg.user` (
`id` INT,
`username` VARCHAR(64),
`password_hash` VARCHAR(64),
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_418343_initial_states') AS relationships;
relationships
Database: db_418343_initial_states

Table Connections:
------------------
(No relationships found)

DROP DATABASE `db_418343_initial_states`;
UNINSTALL EXTENSION vsql_schematron;
