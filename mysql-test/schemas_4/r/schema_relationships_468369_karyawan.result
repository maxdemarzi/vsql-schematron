INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 468369_karyawan
#
CREATE DATABASE `db_468369_karyawan`;
USE `db_468369_karyawan`;
CREATE TABLE `user` (
`id` VARCHAR(50),
`name` VARCHAR(50),
`phone_number` VARCHAR(50),
`email` VARCHAR(50),
`password` VARCHAR(50),
`is_active` BOOLEAN
) CHARACTER SET latin1;
CREATE TABLE `role` (
`role_id` VARCHAR(50),
`user_id` VARCHAR(50),
`role_name` VARCHAR(50)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_468369_karyawan') AS relationships;
relationships
Database: db_468369_karyawan

Table Connections:
------------------
[Implied]  role.user_id -> user.id

DROP DATABASE `db_468369_karyawan`;
UNINSTALL EXTENSION vsql_schematron;
