INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 450382_data
#
CREATE DATABASE `db_450382_data`;
USE `db_450382_data`;
CREATE TABLE `users` (
`user_id` INT,
`username` VARCHAR(64),
`password` VARCHAR(64),
`enabled` INT,
PRIMARY KEY (`user_id`)
) CHARACTER SET latin1;
CREATE TABLE `authorities` (
`role_id` INT,
`username` VARCHAR(64),
`authority` VARCHAR(64),
PRIMARY KEY (`role_id`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_450382_data') AS relationships;
relationships
Database: db_450382_data

Table Connections:
------------------
[Implied]  authorities.username -> users.username

DROP DATABASE `db_450382_data`;
UNINSTALL EXTENSION vsql_schematron;
