INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 412272_create
--echo #
CREATE DATABASE `db_412272_create`;
USE `db_412272_create`;

CREATE TABLE `users` (
  `id` VARCHAR(64),
  `username` VARCHAR(64),
  `password` VARCHAR(64),
  `salt` VARCHAR(64),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `tenants` (
  `id` VARCHAR(64),
  `name` VARCHAR(64),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `car_brands` (
  `id` INT,
  `name` VARCHAR(64),
  `about` VARCHAR(64),
  `tenant_id` VARCHAR(64),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_412272_create') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_412272_create') AS relationships;
DROP DATABASE `db_412272_create`;
UNINSTALL EXTENSION vsql_schematron;
