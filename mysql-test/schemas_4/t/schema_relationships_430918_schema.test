INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 430918_schema
--echo #
CREATE DATABASE `db_430918_schema`;
USE `db_430918_schema`;

CREATE TABLE `department` (
  `id` INT,
  `name` VARCHAR(64),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `roles` (
  `id` INT,
  `department_id` INT,
  `title` VARCHAR(64),
  `salary` DECIMAL(10,2),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `employee` (
  `id` INT,
  `roles_id` INT,
  `first_name` VARCHAR(64),
  `last_name` VARCHAR(64),
  `manager_id` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_430918_schema') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_430918_schema') AS relationships;
DROP DATABASE `db_430918_schema`;
UNINSTALL EXTENSION vsql_schematron;
