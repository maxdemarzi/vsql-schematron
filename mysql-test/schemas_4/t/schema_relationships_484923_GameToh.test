INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 484923_GameToh
--echo #
CREATE DATABASE `db_484923_GameToh`;
USE `db_484923_GameToh`;

CREATE TABLE `aboutus` (
  `name` VARCHAR(255),
  `link` VARCHAR(255),
  PRIMARY KEY (`name`)
) CHARACTER SET latin1;

CREATE TABLE `category` (
  `categoryId` INT,
  `categoryName` VARCHAR(255),
  PRIMARY KEY (`categoryId`)
) CHARACTER SET latin1;

CREATE TABLE `contactus` (
  `msgId` INT,
  `customerId` INT,
  `subject` VARCHAR(255),
  `date` DATETIME,
  `time` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `country` (
  `countryCode` INT,
  `countryName` VARCHAR(255),
  PRIMARY KEY (`countryCode`)
) CHARACTER SET latin1;

CREATE TABLE `credentials` (
  `adminName` VARCHAR(255),
  `passkey` VARCHAR(255),
  PRIMARY KEY (`adminName`)
) CHARACTER SET latin1;

CREATE TABLE `customers` (
  `customerId` INT,
  `contact` VARCHAR(255),
  `fullName` VARCHAR(255),
  `city` VARCHAR(255),
  `bio` VARCHAR(255),
  `twitterHandle` VARCHAR(255),
  `website` VARCHAR(255),
  `email` VARCHAR(255),
  `creationDate` DATETIME,
  PRIMARY KEY (`customerId`)
) CHARACTER SET latin1;

CREATE TABLE `deliverers` (
  `deliverer_id` INT,
  `contact` VARCHAR(255),
  `profile_photo` VARCHAR(255),
  `email` VARCHAR(255),
  `first_name` VARCHAR(255),
  `last_name` VARCHAR(255),
  `city` VARCHAR(255),
  `bio` VARCHAR(255),
  `gender` VARCHAR(255),
  PRIMARY KEY (`deliverer_id`)
) CHARACTER SET latin1;

CREATE TABLE `forfoodies` (
  `name` VARCHAR(255),
  `link` VARCHAR(255),
  PRIMARY KEY (`name`)
) CHARACTER SET latin1;

CREATE TABLE `forrestaurants` (
  `name` VARCHAR(255),
  `link` VARCHAR(255),
  PRIMARY KEY (`name`)
) CHARACTER SET latin1;

CREATE TABLE `menu` (
  `itemId` INT,
  `itemName` VARCHAR(255),
  `restaurantId` INT,
  `photo` VARCHAR(255),
  `price` INT,
  `categoryId` INT,
  `cuisineId` INT,
  `itemStatus` INT,
  `rating` DECIMAL(10,2)
) CHARACTER SET latin1;

CREATE TABLE `ordersummary` (
  `productName` VARCHAR(255),
  `customerId` INT,
  `orderId` INT,
  `delivererId` INT,
  `restaurantId` INT,
  `quantity` INT,
  `price` INT,
  `dateOfOrder` DATETIME,
  `timeOfOrder` DATETIME,
  PRIMARY KEY (`orderId`)
) CHARACTER SET latin1;

CREATE TABLE `restaurants` (
  `restaurantId` INT,
  `contact` VARCHAR(255),
  `nm` VARCHAR(255),
  `street` VARCHAR(255),
  `city` VARCHAR(255),
  `rstate` VARCHAR(255),
  `verified` INT,
  `std` VARCHAR(255),
  `request` DATETIME,
  PRIMARY KEY (`restaurantId`)
) CHARACTER SET latin1;

CREATE TABLE `team` (
  `memberId` INT,
  `memberName` VARCHAR(255),
  `contactNumber` VARCHAR(255),
  `email` VARCHAR(255),
  `profileImage` VARCHAR(255),
  PRIMARY KEY (`memberId`)
) CHARACTER SET latin1;

CREATE TABLE `userprivilages` (
  `name` VARCHAR(255),
  `link` VARCHAR(255),
  PRIMARY KEY (`name`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_484923_GameToh') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_484923_GameToh') AS relationships;
DROP DATABASE `db_484923_GameToh`;
UNINSTALL EXTENSION vsql_schematron;
