INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 403839_Oracle4Quartz
--echo #
CREATE DATABASE `db_403839_Oracle4Quartz`;
USE `db_403839_Oracle4Quartz`;

CREATE TABLE `AIDEV_QRTZ_JOB_DETAILS` (
  `sched_name` VARCHAR(64),
  `job_name` VARCHAR(64),
  `job_group` VARCHAR(64),
  `description` VARCHAR(64),
  `job_class_name` VARCHAR(64),
  `is_durable` VARCHAR(64),
  `is_nonconcurrent` VARCHAR(64),
  `is_update_data` VARCHAR(64),
  `requests_recovery` VARCHAR(64),
  `job_data` VARCHAR(64),
  PRIMARY KEY (`sched_name`, `job_name`, `job_group`)
) CHARACTER SET latin1;

CREATE TABLE `AIDEV_QRTZ_TRIGGERS` (
  `sched_name` VARCHAR(64),
  `trigger_name` VARCHAR(64),
  `trigger_group` VARCHAR(64),
  `job_name` VARCHAR(64),
  `job_group` VARCHAR(64),
  `description` VARCHAR(64),
  `next_fire_time` DECIMAL(10,2),
  `prev_fire_time` DECIMAL(10,2),
  `priority` DECIMAL(10,2),
  `trigger_state` VARCHAR(64),
  `trigger_type` VARCHAR(64),
  `start_time` DECIMAL(10,2),
  `end_time` DECIMAL(10,2),
  `calendar_name` VARCHAR(64),
  `misfire_instr` DECIMAL(10,2),
  `job_data` VARCHAR(64),
  PRIMARY KEY (`sched_name`, `trigger_name`, `trigger_group`)
) CHARACTER SET latin1;

CREATE TABLE `AIDEV_QRTZ_SIMPLE_TRIGGERS` (
  `sched_name` VARCHAR(64),
  `trigger_name` VARCHAR(64),
  `trigger_group` VARCHAR(64),
  `repeat_count` DECIMAL(10,2),
  `repeat_interval` DECIMAL(10,2),
  `times_triggered` DECIMAL(10,2),
  PRIMARY KEY (`sched_name`, `trigger_name`, `trigger_group`)
) CHARACTER SET latin1;

CREATE TABLE `AIDEV_QRTZ_CRON_TRIGGERS` (
  `sched_name` VARCHAR(64),
  `trigger_name` VARCHAR(64),
  `trigger_group` VARCHAR(64),
  `cron_expression` VARCHAR(64),
  `time_zone_id` VARCHAR(64),
  PRIMARY KEY (`sched_name`, `trigger_name`, `trigger_group`)
) CHARACTER SET latin1;

CREATE TABLE `AIDEV_QRTZ_BLOB_TRIGGERS` (
  `sched_name` VARCHAR(64),
  `trigger_name` VARCHAR(64),
  `trigger_group` VARCHAR(64),
  `blob_data` VARCHAR(64),
  PRIMARY KEY (`sched_name`, `trigger_name`, `trigger_group`)
) CHARACTER SET latin1;

CREATE TABLE `AIDEV_QRTZ_CALENDARS` (
  `sched_name` VARCHAR(64),
  `calendar_name` VARCHAR(64),
  `calendar` VARCHAR(64),
  PRIMARY KEY (`sched_name`, `calendar_name`)
) CHARACTER SET latin1;

CREATE TABLE `AIDEV_QRTZ_PAUSED_TRIGGER_GRPS` (
  `sched_name` VARCHAR(64),
  `trigger_group` VARCHAR(64),
  PRIMARY KEY (`sched_name`, `trigger_group`)
) CHARACTER SET latin1;

CREATE TABLE `AIDEV_QRTZ_FIRED_TRIGGERS` (
  `sched_name` VARCHAR(64),
  `entry_id` VARCHAR(64),
  `trigger_name` VARCHAR(64),
  `trigger_group` VARCHAR(64),
  `instance_name` VARCHAR(64),
  `fired_time` DECIMAL(10,2),
  `sched_time` DECIMAL(10,2),
  `priority` DECIMAL(10,2),
  `state` VARCHAR(64),
  `job_name` VARCHAR(64),
  `job_group` VARCHAR(64),
  `is_nonconcurrent` VARCHAR(64),
  `requests_recovery` VARCHAR(64),
  PRIMARY KEY (`sched_name`, `entry_id`)
) CHARACTER SET latin1;

CREATE TABLE `AIDEV_QRTZ_SCHEDULER_STATE` (
  `sched_name` VARCHAR(64),
  `instance_name` VARCHAR(64),
  `last_checkin_time` DECIMAL(10,2),
  `checkin_interval` DECIMAL(10,2),
  PRIMARY KEY (`sched_name`, `instance_name`)
) CHARACTER SET latin1;

CREATE TABLE `AIDEV_QRTZ_LOCKS` (
  `sched_name` VARCHAR(64),
  `lock_name` VARCHAR(64),
  PRIMARY KEY (`sched_name`, `lock_name`)
) CHARACTER SET latin1;

CREATE TABLE `AIDEV_QRTZ_SIMPROP_TRIGGERS` (
  `sched_name` VARCHAR(64),
  `trigger_name` VARCHAR(64),
  `trigger_group` VARCHAR(64),
  `str_prop_1` VARCHAR(64),
  `str_prop_2` VARCHAR(64),
  `str_prop_3` VARCHAR(64),
  `int_prop_1` DECIMAL(10,2),
  `int_prop_2` DECIMAL(10,2),
  `long_prop_1` DECIMAL(10,2),
  `long_prop_2` DECIMAL(10,2),
  `dec_prop_1` DECIMAL(10,2),
  `dec_prop_2` DECIMAL(10,2),
  `bool_prop_1` VARCHAR(64),
  `bool_prop_2` VARCHAR(64),
  PRIMARY KEY (`sched_name`, `trigger_name`, `trigger_group`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_403839_Oracle4Quartz') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_403839_Oracle4Quartz') AS relationships;
DROP DATABASE `db_403839_Oracle4Quartz`;
UNINSTALL EXTENSION vsql_schematron;
