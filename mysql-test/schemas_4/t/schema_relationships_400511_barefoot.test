INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 400511_barefoot
--echo #
CREATE DATABASE `db_400511_barefoot`;
USE `db_400511_barefoot`;

CREATE TABLE `bookmarks` (
  `id` INT,
  `user_id` INT,
  `post_id` INT
) CHARACTER SET latin1;

CREATE TABLE `categories` (
  `id` INT,
  `name` VARCHAR(255),
  `slug` VARCHAR(255),
  `icon` VARCHAR(255),
  `created_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `comments` (
  `id` INT,
  `user_id` INT,
  `post_id` INT,
  `comment` VARCHAR(255),
  `created_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `posts` (
  `id` INT,
  `user_id` INT,
  `title` VARCHAR(255),
  `slug` VARCHAR(255),
  `reading_time` INT,
  `youtube_url` VARCHAR(255),
  `category_id` INT,
  `image` VARCHAR(255),
  `body` VARCHAR(255),
  `is_published` INT,
  `created_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `posts_likes` (
  `id` INT,
  `user_id` INT,
  `post_id` INT,
  `created_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `posts_tags` (
  `id` INT,
  `post_id` INT,
  `tag_id` INT,
  `created_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `socials` (
  `id` INT,
  `user_id` INT,
  `facebook` VARCHAR(255),
  `instagram` VARCHAR(255),
  `twitter` VARCHAR(255),
  `youtube` VARCHAR(255)
) CHARACTER SET latin1;

CREATE TABLE `tags` (
  `id` INT,
  `name` VARCHAR(255),
  `created_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `users` (
  `id` INT,
  `username` VARCHAR(255),
  `email` VARCHAR(255),
  `verified` INT,
  `token` VARCHAR(255),
  `password` VARCHAR(255),
  `profile_image` VARCHAR(255),
  `banner_title` VARCHAR(255),
  `banner_image` VARCHAR(255),
  `created_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_400511_barefoot') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_400511_barefoot') AS relationships;
DROP DATABASE `db_400511_barefoot`;
UNINSTALL EXTENSION vsql_schematron;
