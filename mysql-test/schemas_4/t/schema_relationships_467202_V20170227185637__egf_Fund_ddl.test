INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 467202_V20170227185637__egf_Fund_ddl
--echo #
CREATE DATABASE `db_467202_V20170227185637__egf_Fund_ddl`;
USE `db_467202_V20170227185637__egf_Fund_ddl`;
CREATE TABLE `egf_fund` (
  `id` BIGINT NOT NULL,
  `name` VARCHAR(50),
  `code` VARCHAR(50),
  `identifier` VARCHAR(50),
  `level` BIGINT,
  `parentid` BIGINT,
  `isParent` BOOLEAN,
  `active` BOOLEAN,
  `createdby` BIGINT,
  `createddate` TIMESTAMP NULL,
  `lastmodifiedby` BIGINT,
  `lastmodifieddate` TIMESTAMP NULL,
  `version` BIGINT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_467202_V20170227185637__egf_Fund_ddl') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_467202_V20170227185637__egf_Fund_ddl') AS relationships;
DROP DATABASE `db_467202_V20170227185637__egf_Fund_ddl`;
UNINSTALL EXTENSION vsql_schematron;
