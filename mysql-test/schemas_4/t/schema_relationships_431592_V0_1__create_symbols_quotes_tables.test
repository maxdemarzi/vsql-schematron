INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 431592_V0.1__create_symbols_quotes_tables
--echo #
CREATE DATABASE `db_431592_V0_1__create_symbols_quotes_tables`;
USE `db_431592_V0_1__create_symbols_quotes_tables`;
CREATE TABLE `symbols` (
  `id` INT NOT NULL,
  `value` VARCHAR(50),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `quotes` (
  `id` INT NOT NULL,
  `bid` DECIMAL(10,2),
  `ask` DECIMAL(10,2),
  `last_price` DECIMAL(10,2),
  `volume` DECIMAL(10,2),
  `symbol` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_431592_V0_1__create_symbols_quotes_tables') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_431592_V0_1__create_symbols_quotes_tables') AS relationships;
DROP DATABASE `db_431592_V0_1__create_symbols_quotes_tables`;
UNINSTALL EXTENSION vsql_schematron;
