INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 419734__002_fk_action3
--echo #
CREATE DATABASE `db_419734_002_fk_action3`;
USE `db_419734_002_fk_action3`;

CREATE TABLE `a_tbl` (
  `id` INT,
  `phone` VARCHAR(64),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `b_tbl` (
  `ID` INT,
  `name` VARCHAR(64)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_419734_002_fk_action3') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_419734_002_fk_action3') AS relationships;
DROP DATABASE `db_419734_002_fk_action3`;
UNINSTALL EXTENSION vsql_schematron;
