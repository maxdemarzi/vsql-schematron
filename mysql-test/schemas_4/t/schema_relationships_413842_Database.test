INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 413842_Database
--echo #
CREATE DATABASE `db_413842_Database`;
USE `db_413842_Database`;
CREATE TABLE `department` (
  `departmentName` VARCHAR(50) NOT NULL,
  `deptManager` VARCHAR(50),
  `aisle` INT,
  PRIMARY KEY (`departmentName`)
) CHARACTER SET latin1;
CREATE TABLE `items` (
  `barcode` INT NOT NULL,
  `itemName` VARCHAR(50),
  `departmentName` VARCHAR(50),
  `aisle` INT,
  `deptManager` VARCHAR(50),
  `price` INT,
  `stock` INT,
  PRIMARY KEY (`barcode`)
) CHARACTER SET latin1;
CREATE TABLE `employees` (
  `empID` INT NOT NULL,
  `empName` VARCHAR(50),
  `position` VARCHAR(50),
  `departmentName` VARCHAR(50),
  `shift` VARCHAR(50),
  `salary` INT,
  PRIMARY KEY (`empID`)
) CHARACTER SET latin1;
CREATE TABLE `suppliers` (
  `supplierID` INT NOT NULL,
  `supplierName` VARCHAR(50),
  `supplierAddress` VARCHAR(50),
  `supplierPhoneNumber` VARCHAR(50),
  `other_details` VARCHAR(50),
  PRIMARY KEY (`supplierID`)
) CHARACTER SET latin1;
CREATE TABLE `branches` (
  `branchNumber` VARCHAR(50) NOT NULL,
  `branchManager` VARCHAR(50) NOT NULL,
  `branchName` VARCHAR(50),
  `branchAddress` VARCHAR(50),
  `branchPhoneNumber` VARCHAR(50),
  PRIMARY KEY (`branchNumber`, `branchManager`)
) CHARACTER SET latin1;
CREATE TABLE `transactions` (
  `transactionID` INT NOT NULL,
  `transactionDate` TIMESTAMP NULL,
  `empID` INT,
  `totalPrice` DECIMAL(10,2),
  `paymentMethod` VARCHAR(50),
  PRIMARY KEY (`transactionID`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_413842_Database') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_413842_Database') AS relationships;
DROP DATABASE `db_413842_Database`;
UNINSTALL EXTENSION vsql_schematron;
