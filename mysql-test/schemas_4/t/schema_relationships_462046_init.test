INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 462046_init
--echo #
CREATE DATABASE `db_462046_init`;
USE `db_462046_init`;

CREATE TABLE `OrleansQuery` (
  `QueryKey` VARCHAR(64),
  `QueryText` VARCHAR(64),
  PRIMARY KEY (`QueryKey`)
) CHARACTER SET latin1;

CREATE TABLE `OrleansMembershipVersionTable` (
  `DeploymentId` VARCHAR(64),
  `Timestamp` DATETIME,
  `Version` INT,
  PRIMARY KEY (`DeploymentId`)
) CHARACTER SET latin1;

CREATE TABLE `OrleansMembershipTable` (
  `DeploymentId` VARCHAR(64),
  `Address` VARCHAR(64),
  `Port` INT,
  `Generation` INT,
  `SiloName` VARCHAR(64),
  `HostName` VARCHAR(64),
  `Status` INT,
  `ProxyPort` INT,
  `SuspectTimes` VARCHAR(64),
  `StartTime` DATETIME,
  `IAmAliveTime` DATETIME,
  PRIMARY KEY (`DeploymentId`, `Address`, `Port`, `Generation`)
) CHARACTER SET latin1;

CREATE TABLE `OrleansRemindersTable` (
  `ServiceId` VARCHAR(64),
  `GrainId` VARCHAR(64),
  `ReminderName` VARCHAR(64),
  `StartTime` DATETIME,
  `Period` INT,
  `GrainHash` INT,
  `Version` INT,
  PRIMARY KEY (`ServiceId`, `GrainId`, `ReminderName`)
) CHARACTER SET latin1;

CREATE TABLE `feed` (
  `id` VARCHAR(64),
  `url` VARCHAR(64),
  `title` VARCHAR(64),
  `create_time` DATETIME,
  `update_time` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `article` (
  `id` VARCHAR(64),
  `url` VARCHAR(64),
  `feed_id` VARCHAR(64),
  `title` VARCHAR(64),
  `summary` VARCHAR(64),
  `published` DATETIME,
  `updated` DATETIME,
  `created` DATETIME,
  `keyword` VARCHAR(64),
  `content` VARCHAR(64),
  `contributors` VARCHAR(64),
  `authors` VARCHAR(64),
  `copyright` VARCHAR(64),
  `create_time` DATETIME,
  `update_time` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `subscription` (
  `id` INT,
  `user_id` VARCHAR(64),
  `feed_id` VARCHAR(64),
  `create_time` DATETIME,
  `update_time` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `user` (
  `id` VARCHAR(64),
  `mail` VARCHAR(64),
  `password` VARCHAR(64),
  `salt` VARCHAR(64),
  `create_time` DATETIME,
  `update_time` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `readrecord` (
  `article_id` VARCHAR(64),
  `user_id` VARCHAR(64),
  `create_time` DATETIME,
  `update_time` DATETIME
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_462046_init') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_462046_init') AS relationships;
DROP DATABASE `db_462046_init`;
UNINSTALL EXTENSION vsql_schematron;
