INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 062241_project
--echo #
CREATE DATABASE `db_062241_project`;
USE `db_062241_project`;

CREATE TABLE `user` (
  `userId` VARCHAR(255),
  `email` VARCHAR(255),
  `name` VARCHAR(255),
  `phone` VARCHAR(255),
  `username` VARCHAR(255),
  `website` VARCHAR(255),
  PRIMARY KEY (`userId`)
) CHARACTER SET latin1;

CREATE TABLE `post` (
  `postId` VARCHAR(255),
  `postUserId` VARCHAR(255),
  `body` VARCHAR(255),
  `title` VARCHAR(255),
  PRIMARY KEY (`postId`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_062241_project') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_062241_project') AS relationships;
DROP DATABASE `db_062241_project`;
UNINSTALL EXTENSION vsql_schematron;
