INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 034833_uss_sam_cpy_create_tibero
--echo #
CREATE DATABASE `db_034833_uss_sam_cpy_create_tibero`;
USE `db_034833_uss_sam_cpy_create_tibero`;

CREATE TABLE `COMTECOPSEQ` (
  `TABLE_NAME` VARCHAR(255),
  `NEXT_ID` DECIMAL(10,2),
  PRIMARY KEY (`TABLE_NAME`)
) CHARACTER SET latin1;

CREATE TABLE `COMTNCPYRHTINFO` (
  `CPYRHT_ID` VARCHAR(255),
  `CPYRHT_PRTC_POLICY_CN` VARCHAR(255),
  `FRST_REGIST_PNTTM` DATETIME,
  `FRST_REGISTER_ID` VARCHAR(255),
  `LAST_UPDT_PNTTM` DATETIME,
  `LAST_UPDUSR_ID` VARCHAR(255),
  PRIMARY KEY (`CPYRHT_ID`)
) CHARACTER SET latin1;

CREATE TABLE `COMTCCMMNCLCODE` (
  `CL_CODE` VARCHAR(255),
  `CL_CODE_NM` VARCHAR(255),
  `CL_CODE_DC` VARCHAR(255),
  `USE_AT` VARCHAR(255),
  `FRST_REGIST_PNTTM` DATETIME,
  `FRST_REGISTER_ID` VARCHAR(255),
  `LAST_UPDT_PNTTM` DATETIME,
  `LAST_UPDUSR_ID` VARCHAR(255),
  PRIMARY KEY (`CL_CODE`)
) CHARACTER SET latin1;

CREATE TABLE `COMTCCMMNCODE` (
  `CODE_ID` VARCHAR(255),
  `CODE_ID_NM` VARCHAR(255),
  `CODE_ID_DC` VARCHAR(255),
  `USE_AT` VARCHAR(255),
  `CL_CODE` VARCHAR(255),
  `FRST_REGIST_PNTTM` DATETIME,
  `FRST_REGISTER_ID` VARCHAR(255),
  `LAST_UPDT_PNTTM` DATETIME,
  `LAST_UPDUSR_ID` VARCHAR(255),
  PRIMARY KEY (`CODE_ID`)
) CHARACTER SET latin1;

CREATE TABLE `COMTCCMMNDETAILCODE` (
  `CODE_ID` VARCHAR(255),
  `CODE` VARCHAR(255),
  `CODE_NM` VARCHAR(255),
  `CODE_DC` VARCHAR(255),
  `USE_AT` VARCHAR(255),
  `FRST_REGIST_PNTTM` DATETIME,
  `FRST_REGISTER_ID` VARCHAR(255),
  `LAST_UPDT_PNTTM` DATETIME,
  `LAST_UPDUSR_ID` VARCHAR(255),
  PRIMARY KEY (`CODE_ID`, `CODE`)
) CHARACTER SET latin1;

CREATE TABLE `COMTNAUTHORGROUPINFO` (
  `GROUP_ID` VARCHAR(255),
  `GROUP_NM` VARCHAR(255),
  `GROUP_CREAT_DE` VARCHAR(255),
  `GROUP_DC` VARCHAR(255),
  PRIMARY KEY (`GROUP_ID`)
) CHARACTER SET latin1;

CREATE TABLE `COMTNORGNZTINFO` (
  `ORGNZT_ID` VARCHAR(255),
  `ORGNZT_NM` VARCHAR(255),
  `ORGNZT_DC` VARCHAR(255),
  PRIMARY KEY (`ORGNZT_ID`)
) CHARACTER SET latin1;

CREATE TABLE `COMTNFILE` (
  `ATCH_FILE_ID` VARCHAR(255),
  `CREAT_DT` DATETIME,
  `USE_AT` VARCHAR(255),
  PRIMARY KEY (`ATCH_FILE_ID`)
) CHARACTER SET latin1;

CREATE TABLE `COMTNFILEDETAIL` (
  `ATCH_FILE_ID` VARCHAR(255),
  `FILE_SN` DECIMAL(10,2),
  `FILE_STRE_COURS` VARCHAR(255),
  `STRE_FILE_NM` VARCHAR(255),
  `ORIGNL_FILE_NM` VARCHAR(255),
  `FILE_EXTSN` VARCHAR(255),
  `FILE_CN` VARCHAR(255),
  `FILE_SIZE` DECIMAL(10,2),
  PRIMARY KEY (`ATCH_FILE_ID`, `FILE_SN`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_034833_uss_sam_cpy_create_tibero') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_034833_uss_sam_cpy_create_tibero') AS relationships;
DROP DATABASE `db_034833_uss_sam_cpy_create_tibero`;
UNINSTALL EXTENSION vsql_schematron;
