INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 099303_latest_electro_v_6
--echo #
CREATE DATABASE `db_099303_latest_electro_v_6`;
USE `db_099303_latest_electro_v_6`;

CREATE TABLE `customer` (
  `Cust_id` INT,
  `First_name` VARCHAR(255),
  `Last_name` VARCHAR(255),
  `Email` VARCHAR(255),
  `Password` VARCHAR(255),
  `Role` VARCHAR(255),
  `Profile_pic` VARCHAR(255),
  PRIMARY KEY (`Cust_id`)
) CHARACTER SET latin1;

CREATE TABLE `ebill_monthly_plan` (
  `bill_id` INT,
  `Total_cost_tou` DECIMAL(10,2),
  `Cost_day_time` DECIMAL(10,2),
  `Cost_off_peak_time` DECIMAL(10,2),
  `Cost_peak_time` DECIMAL(10,2),
  `Total_cost_fixed` DECIMAL(10,2),
  `Units_day_time` INT,
  `Units_off_peak_time` INT,
  `Units_peak_time` INT,
  `Total_units` INT,
  `Best_model` VARCHAR(255),
  `Cust_id` INT
) CHARACTER SET latin1;

CREATE TABLE `ebill_special_event` (
  `bill_id` INT,
  `bill_plan_name` VARCHAR(255),
  `bill_model` VARCHAR(255),
  `Total_units` DECIMAL(10,2),
  `Cust_id` INT
) CHARACTER SET latin1;

CREATE TABLE `ebill_special_event_tou` (
  `bill_id` INT,
  `Total_cost_tou` DECIMAL(10,2),
  `Cost_day_time` DECIMAL(10,2),
  `Cost_off_peak_time` DECIMAL(10,2),
  `Cost_peak_time` DECIMAL(10,2),
  `Units_day_time` DECIMAL(10,2),
  `Units_off_peak_time` DECIMAL(10,2),
  `Units_peak_time` DECIMAL(10,2),
  `Cust_id` INT
) CHARACTER SET latin1;

CREATE TABLE `electric_device_mplan` (
  `device_id` INT,
  `bill_id` INT,
  `appliance` VARCHAR(255),
  `quantity` INT,
  `hPeak` INT,
  `mPeak` INT,
  `hOffPeak` INT,
  `mOffPeak` INT,
  `hDay` INT,
  `mDay` INT,
  `priority` VARCHAR(255),
  `using_minutes_peak_time` DECIMAL(10,2),
  `using_minutes_off_peak_time` DECIMAL(10,2),
  `using_minutes_day_time` DECIMAL(10,2),
  `power` DECIMAL(10,2),
  `total_units` DECIMAL(10,2),
  `units_peak_time` DECIMAL(10,2),
  `units_off_peak_time` DECIMAL(10,2),
  `units_day_time` DECIMAL(10,2),
  `total_cost_TOU` DECIMAL(10,2),
  `cost_peak_time` DECIMAL(10,2),
  `cost_off_peak_time` DECIMAL(10,2),
  `cost_day_time` DECIMAL(10,2),
  `Cust_id` INT
) CHARACTER SET latin1;

CREATE TABLE `electric_device_special_event_fixed` (
  `device_id` INT,
  `bill_id` INT,
  `appliance` VARCHAR(255),
  `quantity` INT,
  `hfixed` INT,
  `mfixed` INT,
  `using_minutes_fixed` INT,
  `power` DECIMAL(10,2),
  `total_units_fixed` DECIMAL(10,2),
  `numberOfDays` INT,
  `Cust_id` INT
) CHARACTER SET latin1;

CREATE TABLE `electric_device_special_event_tou` (
  `device_id` INT,
  `bill_id` INT,
  `appliance` VARCHAR(255),
  `quantity` INT,
  `hPeak` INT,
  `mPeak` INT,
  `hOffPeak` INT,
  `mOffPeak` INT,
  `hDay` INT,
  `mDay` INT,
  `using_minutes_peak_time` DECIMAL(10,2),
  `using_minutes_off_peak_time` DECIMAL(10,2),
  `using_minutes_day_time` DECIMAL(10,2),
  `power` INT,
  `units_peak_time` DECIMAL(10,2),
  `units_off_peak_time` DECIMAL(10,2),
  `units_day_time` DECIMAL(10,2),
  `total_cost_TOU` DECIMAL(10,2),
  `cost_peak_time` DECIMAL(10,2),
  `cost_off_peak_time` DECIMAL(10,2),
  `cost_day_time` DECIMAL(10,2),
  `numberOfDays` INT,
  `total_units` DECIMAL(10,2),
  `Cust_id` INT
) CHARACTER SET latin1;

CREATE TABLE `employee` (
  `Emp_id` INT,
  `First_name` VARCHAR(255),
  `Last_name` VARCHAR(255),
  `Email` VARCHAR(255),
  `Conatact_no` VARCHAR(255),
  `Password` VARCHAR(255),
  `NIC` VARCHAR(255),
  `Address` VARCHAR(255),
  `Enrollment_date` DATETIME,
  `Role` VARCHAR(255)
) CHARACTER SET latin1;

CREATE TABLE `fixed_ucharge` (
  `Unit_category` VARCHAR(255),
  `Unit_charge` DECIMAL(10,2),
  `Update_unit_charges` DECIMAL(10,2),
  `Update_ucharge_status` INT,
  `Unit_charges_requested_date` VARCHAR(255),
  `Unit_charges_accepted_date` VARCHAR(255),
  `Fixed_charge` DECIMAL(10,2),
  `Update_fixed_charges` DECIMAL(10,2),
  `Update_fcharge_status` INT,
  `Fixed_charges_requested_date` VARCHAR(255),
  `Fixed_charges_accepted_date` VARCHAR(255),
  PRIMARY KEY (`Unit_category`)
) CHARACTER SET latin1;

CREATE TABLE `suggestions` (
  `suggest_id` INT,
  `device_id` INT,
  `bill_id` INT,
  `appliance` VARCHAR(255),
  `priority` VARCHAR(255),
  `quantity` INT,
  `cur_time` VARCHAR(255),
  `change_time` VARCHAR(255),
  `can_change_hours` INT,
  `can_change_minutes` INT,
  `save_amount` DECIMAL(10,2),
  `total_cost_TOU` DECIMAL(10,2),
  `Cust_id` INT
) CHARACTER SET latin1;

CREATE TABLE `tou_ucharge` (
  `Time_category` VARCHAR(255),
  `Unit_charge` DECIMAL(10,2),
  `Update_unit_charges` DECIMAL(10,2),
  `Update_ucharge_status` INT,
  `Unit_charges_requested_date` VARCHAR(255),
  `Unit_charges_accepted_date` VARCHAR(255),
  `Fixed_charge` DECIMAL(10,2),
  `Update_fixed_charges` DECIMAL(10,2),
  `Update_fcharge_status` INT,
  `Fixed_charges_requested_date` VARCHAR(255),
  `Fixed_charges_accepted_date` VARCHAR(255),
  PRIMARY KEY (`Time_category`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_099303_latest_electro_v_6') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_099303_latest_electro_v_6') AS relationships;
DROP DATABASE `db_099303_latest_electro_v_6`;
UNINSTALL EXTENSION vsql_schematron;
