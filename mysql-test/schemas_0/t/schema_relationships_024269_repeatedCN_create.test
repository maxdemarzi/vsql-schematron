INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 024269_repeatedCN_create
--echo #
CREATE DATABASE `db_024269_repeatedCN_create`;
USE `db_024269_repeatedCN_create`;
CREATE TABLE `professors` (
  `prof_id` INT NOT NULL,
  `first_name` VARCHAR(50),
  `last_name` VARCHAR(50),
  `nickname` VARCHAR(50),
  PRIMARY KEY (`prof_id`)
) CHARACTER SET latin1;
CREATE TABLE `teaching` (
  `course_id` VARCHAR(50),
  `prof_id` INT
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_024269_repeatedCN_create') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_024269_repeatedCN_create') AS relationships;
DROP DATABASE `db_024269_repeatedCN_create`;
UNINSTALL EXTENSION vsql_schematron;
