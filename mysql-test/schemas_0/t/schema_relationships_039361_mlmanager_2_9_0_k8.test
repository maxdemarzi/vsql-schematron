INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 039361_mlmanager_2.9.0-k8
--echo #
CREATE DATABASE `db_039361_mlmanager_2_9_0_k8`;
USE `db_039361_mlmanager_2_9_0_k8`;

CREATE TABLE `FEATURESTORE.DEPLOYMENT_HISTORY_TMP` (
  `MODEL_SCHEMA_NAME` VARCHAR(64),
  `MODEL_TABLE_NAME` VARCHAR(64),
  `ASOF_TS` DATETIME,
  `TRAINING_SET_ID` INT,
  `TRAINING_SET_VERSION` INT,
  `RUN_ID` VARCHAR(64),
  `LAST_UPDATE_TS` DATETIME,
  `LAST_UPDATE_USERNAME` VARCHAR(64),
  PRIMARY KEY (`MODEL_SCHEMA_NAME`, `MODEL_TABLE_NAME`, `ASOF_TS`)
) CHARACTER SET latin1;

CREATE TABLE `FEATURESTORE.DEPLOYMENT_TMP` (
  `MODEL_SCHEMA_NAME` VARCHAR(64),
  `MODEL_TABLE_NAME` VARCHAR(64),
  `TRAINING_SET_ID` INT,
  `TRAINING_SET_VERSION` INT,
  `RUN_ID` VARCHAR(64),
  `LAST_UPDATE_TS` DATETIME,
  `LAST_UPDATE_USERNAME` VARCHAR(64),
  PRIMARY KEY (`MODEL_SCHEMA_NAME`, `MODEL_TABLE_NAME`)
) CHARACTER SET latin1;

CREATE TABLE `FEATURESTORE.TRAINING_SET_FEATURE_STATS_TMP` (
  `TRAINING_SET_ID` INT,
  `TRAINING_SET_VERSION` INT,
  `FEATURE_ID` INT,
  `FEATURE_CARDINALITY` INT,
  `FEATURE_HISTOGRAM` VARCHAR(64),
  `FEATURE_MEAN` DECIMAL(10,2),
  `FEATURE_MEDIAN` DECIMAL(10,2),
  `FEATURE_Q1` DECIMAL(10,2),
  `FEATURE_Q3` DECIMAL(10,2),
  `FEATURE_MIN` DECIMAL(10,2),
  `FEATURE_MAX` DECIMAL(10,2),
  `FEATURE_COUNT` INT,
  `FEATURE_STDDEV` DECIMAL(10,2),
  `LAST_UPDATE_TS` DATETIME,
  `LAST_UPDATE_USERNAME` VARCHAR(64),
  PRIMARY KEY (`TRAINING_SET_ID`, `TRAINING_SET_VERSION`, `FEATURE_ID`)
) CHARACTER SET latin1;

CREATE TABLE `FEATURESTORE.TRAINING_SET_LABEL_STATS_TMP` (
  `TRAINING_SET_ID` INT,
  `TRAINING_SET_VERSION` INT,
  `LABEL_COLUMN` INT,
  `LABEL_CARDINALITY` INT,
  `LABEL_HISTOGRAM` VARCHAR(64),
  `LABEL_MEAN` DECIMAL(10,2),
  `LABEL_MEDIAN` DECIMAL(10,2),
  `LABEL_Q1` DECIMAL(10,2),
  `LABEL_Q3` DECIMAL(10,2),
  `LABEL_MIN` DECIMAL(10,2),
  `LABEL_MAX` DECIMAL(10,2),
  `LABEL_COUNT` INT,
  `LABEL_STDDEV` DECIMAL(10,2),
  `LAST_UPDATE_TS` DATETIME,
  `LAST_UPDATE_USERNAME` VARCHAR(64),
  PRIMARY KEY (`TRAINING_SET_ID`, `TRAINING_SET_VERSION`, `LABEL_COLUMN`)
) CHARACTER SET latin1;

CREATE TABLE `FEATURESTORE.TRAINING_SET_INSTANCE_TMP` (
  `TRAINING_SET_ID` INT,
  `TRAINING_SET_VERSION` INT,
  `TRAINING_SET_START_TS` DATETIME,
  `TRAINING_SET_END_TS` DATETIME,
  `TRAINING_SET_CREATE_TS` DATETIME,
  `LAST_UPDATE_TS` DATETIME,
  `LAST_UPDATE_USERNAME` VARCHAR(64),
  PRIMARY KEY (`TRAINING_SET_ID`, `TRAINING_SET_VERSION`)
) CHARACTER SET latin1;

CREATE TABLE `FEATURESTORE.TRAINING_SET_FEATURE_TMP` (
  `TRAINING_SET_ID` INT,
  `FEATURE_ID` INT,
  `FEATURE_SET_ID` INT,
  `FEATURE_SET_VERSION` INT,
  `IS_LABEL` INT,
  `LAST_UPDATE_TS` DATETIME,
  `LAST_UPDATE_USERNAME` VARCHAR(64),
  PRIMARY KEY (`TRAINING_SET_ID`, `FEATURE_ID`)
) CHARACTER SET latin1;

CREATE TABLE `FEATURESTORE.TRAINING_VIEW_VERSION` (
  `VIEW_ID` INT,
  `VIEW_VERSION` INT,
  `SQL_TEXT` VARCHAR(64),
  `LABEL_COLUMN` VARCHAR(64),
  `TS_COLUMN` VARCHAR(64),
  `LAST_UPDATE_TS` DATETIME,
  `LAST_UPDATE_USERNAME` VARCHAR(64),
  PRIMARY KEY (`VIEW_ID`, `VIEW_VERSION`)
) CHARACTER SET latin1;

CREATE TABLE `FEATURESTORE.TRAINING_VIEW_KEY_TMP` (
  `VIEW_ID` INT,
  `VIEW_VERSION` INT,
  `KEY_COLUMN_NAME` VARCHAR(64),
  `KEY_TYPE` VARCHAR(64),
  `LAST_UPDATE_TS` DATETIME,
  `LAST_UPDATE_USERNAME` VARCHAR(64),
  PRIMARY KEY (`VIEW_ID`, `VIEW_VERSION`, `KEY_COLUMN_NAME`, `KEY_TYPE`)
) CHARACTER SET latin1;

CREATE TABLE `FEATURESTORE.FEATURE_SET_VERSION` (
  `FEATURE_SET_ID` INT,
  `FEATURE_SET_VERSION` INT,
  `DEPLOYED` INT,
  `DEPLOY_TS` DATETIME,
  `CREATE_TS` DATETIME,
  `CREATE_USERNAME` VARCHAR(64),
  PRIMARY KEY (`FEATURE_SET_ID`, `FEATURE_SET_VERSION`)
) CHARACTER SET latin1;

CREATE TABLE `FEATURESTORE.FEATURE_SET_KEY_TMP` (
  `FEATURE_SET_ID` INT,
  `FEATURE_SET_VERSION` INT,
  `KEY_COLUMN_NAME` VARCHAR(64),
  `KEY_COLUMN_DATA_TYPE` VARCHAR(64),
  `LAST_UPDATE_TS` DATETIME,
  `LAST_UPDATE_USERNAME` VARCHAR(64),
  PRIMARY KEY (`FEATURE_SET_ID`, `FEATURE_SET_VERSION`, `KEY_COLUMN_NAME`)
) CHARACTER SET latin1;

CREATE TABLE `FEATURESTORE.FEATURE_VERSION` (
  `FEATURE_ID` INT,
  `FEATURE_SET_ID` INT,
  `FEATURE_SET_VERSION` INT,
  PRIMARY KEY (`FEATURE_ID`, `FEATURE_SET_ID`, `FEATURE_SET_VERSION`)
) CHARACTER SET latin1;

CREATE TABLE `FEATURESTORE.PIPELINE_OPS_TMP` (
  `FEATURE_SET_ID` INT,
  `EXTRACT_UP_TO_TS` DATETIME,
  PRIMARY KEY (`FEATURE_SET_ID`)
) CHARACTER SET latin1;

CREATE TABLE `FEATURESTORE.PIPELINE_AGG_TMP` (
  `FEATURE_SET_ID` INT,
  `FEATURE_NAME_PREFIX` VARCHAR(64),
  `COLUMN_NAME` VARCHAR(64),
  `AGG_FUNCTIONS` VARCHAR(64),
  `AGG_WINDOWS` VARCHAR(64),
  `AGG_DEFAULT_VALUE` DECIMAL(10,2),
  `LAST_UPDATE_TS` DATETIME,
  `LAST_UPDATE_USERNAME` VARCHAR(64),
  PRIMARY KEY (`FEATURE_SET_ID`, `FEATURE_NAME_PREFIX`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_039361_mlmanager_2_9_0_k8') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_039361_mlmanager_2_9_0_k8') AS relationships;
DROP DATABASE `db_039361_mlmanager_2_9_0_k8`;
UNINSTALL EXTENSION vsql_schematron;
