INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 016656_tables_hsqldb
--echo #
CREATE DATABASE `db_016656_tables_hsqldb`;
USE `db_016656_tables_hsqldb`;

CREATE TABLE `qrtz_job_details` (
  `JOB_NAME` VARCHAR(255),
  `JOB_GROUP` VARCHAR(255),
  `DESCRIPTION` VARCHAR(255),
  `JOB_CLASS_NAME` VARCHAR(255),
  `IS_DURABLE` VARCHAR(255),
  `IS_VOLATILE` VARCHAR(255),
  `IS_STATEFUL` VARCHAR(255),
  `REQUESTS_RECOVERY` VARCHAR(255),
  `JOB_DATA` VARCHAR(255),
  PRIMARY KEY (`JOB_NAME`, `JOB_GROUP`)
) CHARACTER SET latin1;

CREATE TABLE `qrtz_job_listeners` (
  `JOB_NAME` VARCHAR(255),
  `JOB_GROUP` VARCHAR(255),
  `JOB_LISTENER` VARCHAR(255),
  PRIMARY KEY (`JOB_NAME`, `JOB_GROUP`, `JOB_LISTENER`)
) CHARACTER SET latin1;

CREATE TABLE `qrtz_triggers` (
  `TRIGGER_NAME` VARCHAR(255),
  `TRIGGER_GROUP` VARCHAR(255),
  `JOB_NAME` VARCHAR(255),
  `JOB_GROUP` VARCHAR(255),
  `IS_VOLATILE` VARCHAR(255),
  `DESCRIPTION` VARCHAR(255),
  `NEXT_FIRE_TIME` DECIMAL(10,2),
  `PREV_FIRE_TIME` DECIMAL(10,2),
  `PRIORITY` INT,
  `TRIGGER_STATE` VARCHAR(255),
  `TRIGGER_TYPE` VARCHAR(255),
  `START_TIME` DECIMAL(10,2),
  `END_TIME` DECIMAL(10,2),
  `CALENDAR_NAME` VARCHAR(255),
  `MISFIRE_INSTR` DECIMAL(10,2),
  `JOB_DATA` VARCHAR(255),
  PRIMARY KEY (`TRIGGER_NAME`, `TRIGGER_GROUP`)
) CHARACTER SET latin1;

CREATE TABLE `qrtz_simple_triggers` (
  `TRIGGER_NAME` VARCHAR(255),
  `TRIGGER_GROUP` VARCHAR(255),
  `REPEAT_COUNT` DECIMAL(10,2),
  `REPEAT_INTERVAL` DECIMAL(10,2),
  `TIMES_TRIGGERED` DECIMAL(10,2),
  PRIMARY KEY (`TRIGGER_NAME`, `TRIGGER_GROUP`)
) CHARACTER SET latin1;

CREATE TABLE `qrtz_cron_triggers` (
  `TRIGGER_NAME` VARCHAR(255),
  `TRIGGER_GROUP` VARCHAR(255),
  `CRON_EXPRESSION` VARCHAR(255),
  `TIME_ZONE_ID` VARCHAR(255),
  PRIMARY KEY (`TRIGGER_NAME`, `TRIGGER_GROUP`)
) CHARACTER SET latin1;

CREATE TABLE `qrtz_blob_triggers` (
  `TRIGGER_NAME` VARCHAR(255),
  `TRIGGER_GROUP` VARCHAR(255),
  `LONGVARBINARY_DATA` VARCHAR(255),
  PRIMARY KEY (`TRIGGER_NAME`, `TRIGGER_GROUP`)
) CHARACTER SET latin1;

CREATE TABLE `qrtz_trigger_listeners` (
  `TRIGGER_NAME` VARCHAR(255),
  `TRIGGER_GROUP` VARCHAR(255),
  `TRIGGER_LISTENER` VARCHAR(255),
  PRIMARY KEY (`TRIGGER_NAME`, `TRIGGER_GROUP`, `TRIGGER_LISTENER`)
) CHARACTER SET latin1;

CREATE TABLE `qrtz_calendars` (
  `CALENDAR_NAME` VARCHAR(255),
  `CALENDAR` VARCHAR(255),
  PRIMARY KEY (`CALENDAR_NAME`)
) CHARACTER SET latin1;

CREATE TABLE `qrtz_paused_trigger_grps` (
  `TRIGGER_GROUP` VARCHAR(255),
  PRIMARY KEY (`TRIGGER_GROUP`)
) CHARACTER SET latin1;

CREATE TABLE `qrtz_fired_triggers` (
  `ENTRY_ID` VARCHAR(255),
  `TRIGGER_NAME` VARCHAR(255),
  `TRIGGER_GROUP` VARCHAR(255),
  `IS_VOLATILE` VARCHAR(255),
  `INSTANCE_NAME` VARCHAR(255),
  `FIRED_TIME` DECIMAL(10,2),
  `PRIORITY` INT,
  `STATE` VARCHAR(255),
  `JOB_NAME` VARCHAR(255),
  `JOB_GROUP` VARCHAR(255),
  `IS_STATEFUL` VARCHAR(255),
  `REQUESTS_RECOVERY` VARCHAR(255),
  PRIMARY KEY (`ENTRY_ID`)
) CHARACTER SET latin1;

CREATE TABLE `qrtz_scheduler_state` (
  `INSTANCE_NAME` VARCHAR(255),
  `LAST_CHECKIN_TIME` DECIMAL(10,2),
  `CHECKIN_INTERVAL` DECIMAL(10,2),
  PRIMARY KEY (`INSTANCE_NAME`)
) CHARACTER SET latin1;

CREATE TABLE `qrtz_locks` (
  `LOCK_NAME` VARCHAR(255),
  PRIMARY KEY (`LOCK_NAME`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_016656_tables_hsqldb') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_016656_tables_hsqldb') AS relationships;
DROP DATABASE `db_016656_tables_hsqldb`;
UNINSTALL EXTENSION vsql_schematron;
