INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 049849_migration
--echo #
CREATE DATABASE `db_049849_migration`;
USE `db_049849_migration`;

CREATE TABLE `users` (
  `id` VARCHAR(64),
  `email` VARCHAR(64),
  `passwordHash` VARCHAR(64),
  `firstName` VARCHAR(64),
  `middleName` VARCHAR(64),
  `lastName` VARCHAR(64),
  `profilePicture` VARCHAR(64),
  `createdAt` DATETIME,
  `updatedAt` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `books` (
  `id` VARCHAR(64),
  `title` VARCHAR(64),
  `isbn` INT,
  `description` VARCHAR(64),
  `price` DECIMAL(10,2),
  `coverUrl` VARCHAR(64),
  `createdAt` DATETIME,
  `updatedAt` DATETIME,
  `transactionId` VARCHAR(64),
  `publisherId` VARCHAR(64),
  `sold` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `authors` (
  `id` VARCHAR(64),
  `firstName` VARCHAR(64),
  `middleName` VARCHAR(64),
  `lastName` VARCHAR(64),
  `description` VARCHAR(64),
  `createdAt` DATETIME,
  `updatedAt` DATETIME,
  `bookId` VARCHAR(64),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `publishers` (
  `id` VARCHAR(64),
  `name` VARCHAR(64),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `genres` (
  `id` INT,
  `name` VARCHAR(64),
  `bookId` VARCHAR(64),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `ratings` (
  `id` VARCHAR(64),
  `value` INT,
  `description` VARCHAR(64),
  `createdAt` DATETIME,
  `updatedAt` DATETIME,
  `userId` VARCHAR(64),
  `bookId` VARCHAR(64),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `addresses` (
  `id` VARCHAR(64),
  `street` VARCHAR(64),
  `apartmentOrUnit` VARCHAR(64),
  `city` VARCHAR(64),
  `state` VARCHAR(64),
  `country` VARCHAR(64),
  `zipcode` INT,
  `userShippingAddressId` VARCHAR(64),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `transactions` (
  `id` VARCHAR(64),
  `userId` VARCHAR(64),
  `booksId` VARCHAR(64),
  `createdAt` DATETIME,
  `updatedAt` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `credit_cards` (
  `cardId` VARCHAR(64),
  `userId` VARCHAR(64),
  `encryptedCreditCardNumber` VARCHAR(64),
  `encryptedCCV` VARCHAR(64),
  `createdAt` DATETIME,
  `updatedAt` DATETIME,
  PRIMARY KEY (`cardId`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_049849_migration') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_049849_migration') AS relationships;
DROP DATABASE `db_049849_migration`;
UNINSTALL EXTENSION vsql_schematron;
