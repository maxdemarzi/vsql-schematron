INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 028747_general_schema.in
--echo #
CREATE DATABASE `db_028747_general_schema_in`;
USE `db_028747_general_schema_in`;

CREATE TABLE `orders` (
  `id` INT,
  `user_id` INT,
  `status` VARCHAR(64),
  `created_at` VARCHAR(64),
  `modified_at` DATETIME,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `order_items` (
  `order_id` INT,
  `product_id` INT,
  `quantity` INT,
  `created_at` DATETIME,
  `modified_at` DATETIME
) CHARACTER SET latin1;

CREATE TABLE `products` (
  `id` INT,
  `name` VARCHAR(64),
  `merchant_id` INT,
  `price` INT,
  `status` VARCHAR(64),
  `created_at` DATETIME,
  `modified_at` DATETIME,
  PRIMARY KEY (`id`, `name`)
) CHARACTER SET latin1;

CREATE TABLE `users` (
  `id` INT,
  `full_name` VARCHAR(64),
  `email` VARCHAR(64),
  `gender` VARCHAR(64),
  `date_of_birth` VARCHAR(64),
  `created_at` VARCHAR(64),
  `modified_at` DATETIME,
  `country_code` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `merchants` (
  `id` INT,
  `merchant_name` VARCHAR(64),
  `country_code` INT,
  `created_at` VARCHAR(64),
  `modified_at` DATETIME,
  `admin_id` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `countries` (
  `code` INT,
  `name` VARCHAR(64),
  `continent_name` VARCHAR(64),
  PRIMARY KEY (`code`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_028747_general_schema_in') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_028747_general_schema_in') AS relationships;
DROP DATABASE `db_028747_general_schema_in`;
UNINSTALL EXTENSION vsql_schematron;
