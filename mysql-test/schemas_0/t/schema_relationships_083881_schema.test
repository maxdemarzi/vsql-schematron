INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 083881_schema
--echo #
CREATE DATABASE `db_083881_schema`;
USE `db_083881_schema`;
CREATE TABLE `schema_migrations` (
  `version` VARCHAR(50) NOT NULL,
  PRIMARY KEY (`version`)
) CHARACTER SET latin1;
CREATE TABLE `users` (
  `id` INT NOT NULL,
  `email` TEXT,
  `is_admin` BOOLEAN,
  `hashed_password` TEXT,
  `joined` TEXT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `apis` (
  `id` INT NOT NULL,
  `title` TEXT,
  `endpoints` TEXT,
  `user_id` INT,
  `created` TEXT,
  `updated` TEXT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_083881_schema') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_083881_schema') AS relationships;
DROP DATABASE `db_083881_schema`;
UNINSTALL EXTENSION vsql_schematron;
