INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 011015_ncc
#
CREATE DATABASE `db_011015_ncc`;
USE `db_011015_ncc`;
CREATE TABLE `users` (
`id` INT,
`username` VARCHAR(64),
`email` VARCHAR(64),
`password` VARCHAR(64),
PRIMARY KEY (`username`)
) CHARACTER SET latin1;
CREATE TABLE `event` (
`event_id` INT,
`event_username` VARCHAR(64),
`event_name` VARCHAR(64),
`event_desc` VARCHAR(64),
`event_date` DATETIME,
`time` VARCHAR(64),
`venue` VARCHAR(64),
`status1` VARCHAR(64),
PRIMARY KEY (`event_id`)
) CHARACTER SET latin1;
CREATE TABLE `eventJoin` (
`event_id` INT,
`username` VARCHAR(64),
`status2` VARCHAR(64),
PRIMARY KEY (`event_id`, `username`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_011015_ncc') AS relationships;
relationships
Database: db_011015_ncc

Table Connections:
------------------
[Implied]  event.event_username -> users.username
[Implied]  eventJoin.event_id -> event.event_id
[Implied]  eventJoin.username -> users.username

DROP DATABASE `db_011015_ncc`;
UNINSTALL EXTENSION vsql_schematron;
