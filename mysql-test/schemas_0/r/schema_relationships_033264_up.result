INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 033264_up
#
CREATE DATABASE `db_033264_up`;
USE `db_033264_up`;
CREATE TABLE `users` (
`id` VARCHAR(64),
`username` VARCHAR(64),
`host` VARCHAR(64),
`nickname` VARCHAR(64),
`uri` VARCHAR(64),
`inbox` VARCHAR(64),
`public_key` VARCHAR(64),
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `users_auth` (
`username` VARCHAR(64),
`encrypted_password` VARCHAR(64),
`users_id` VARCHAR(64),
`private_key` VARCHAR(64),
`public_key` VARCHAR(64),
PRIMARY KEY (`username`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_033264_up') AS relationships;
relationships
Database: db_033264_up

Table Connections:
------------------
[Implied]  users.username -> users_auth.username
[Implied]  users_auth.username -> users.username
[Implied]  users_auth.users_id -> users.id

DROP DATABASE `db_033264_up`;
UNINSTALL EXTENSION vsql_schematron;
