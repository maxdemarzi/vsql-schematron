INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 060460_chatbot
#
CREATE DATABASE `db_060460_chatbot`;
USE `db_060460_chatbot`;
CREATE TABLE `dotaz` (
`id` INT,
`odpoved_id` INT,
`text` TEXT,
`priorita` INT
) CHARACTER SET latin1;
CREATE TABLE `historie` (
`id` INT NOT NULL,
`message` TEXT,
`date` DATETIME,
`type` VARCHAR(50),
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `odpoved` (
`id` INT NOT NULL,
`text` TEXT,
PRIMARY KEY (`id`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_060460_chatbot') AS relationships;
relationships
Database: db_060460_chatbot

Table Connections:
------------------
[Implied]  dotaz.odpoved_id -> odpoved.id

DROP DATABASE `db_060460_chatbot`;
UNINSTALL EXTENSION vsql_schematron;
