INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 144829_V0.33__alter_cloud_provider_parameters
--echo #
CREATE DATABASE `db_144829_V0_33__alter_cloud_provider_parameters`;
USE `db_144829_V0_33__alter_cloud_provider_parameters`;
CREATE TABLE `cloud_provider_params_copy` (
  `id` BIGINT NOT NULL,
  `name` VARCHAR(50),
  `cloud_provider` VARCHAR(50),
  `account_id` BIGINT,
  `cloud_provider_params_reference` VARCHAR(50),
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
CREATE TABLE `cloud_provider_params_copy_field` (
  `id` BIGINT NOT NULL,
  `key` VARCHAR(50),
  `value` VARCHAR(50),
  `cloud_provider_params_copy_id` BIGINT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;
--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_144829_V0_33__alter_cloud_provider_parameters') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_144829_V0_33__alter_cloud_provider_parameters') AS relationships;
DROP DATABASE `db_144829_V0_33__alter_cloud_provider_parameters`;
UNINSTALL EXTENSION vsql_schematron;
