INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 137919_instawltdb
--echo #
CREATE DATABASE `db_137919_instawltdb`;
USE `db_137919_instawltdb`;

CREATE TABLE `ErrorLog` (
  `ErrorLogID` INT,
  `ErrorTime` DATETIME,
  `UserName` VARCHAR(255),
  `ErrorNumber` INT,
  `ErrorSeverity` INT,
  `ErrorState` INT,
  `ErrorProcedure` VARCHAR(255),
  `ErrorLine` INT,
  `ErrorMessage` VARCHAR(255),
  PRIMARY KEY (`ErrorLogID`)
) CHARACTER SET latin1;

CREATE TABLE `BuildVersion` (
  `SystemInformationID` INT,
  `Database Version` VARCHAR(255),
  `VersionDate` DATETIME,
  `ModifiedDate` DATETIME,
  PRIMARY KEY (`SystemInformationID`)
) CHARACTER SET latin1;

CREATE TABLE `Address` (
  `AddressID` INT,
  `AddressLine1` VARCHAR(255),
  `AddressLine2` VARCHAR(255),
  `City` VARCHAR(255),
  `StateProvince` VARCHAR(255),
  `CountryRegion` VARCHAR(255),
  `PostalCode` VARCHAR(255),
  `rowguid` VARCHAR(255),
  `ModifiedDate` DATETIME,
  PRIMARY KEY (`AddressID`)
) CHARACTER SET latin1;

CREATE TABLE `Customer` (
  `CustomerID` INT,
  `NameStyle` INT,
  `Title` VARCHAR(255),
  `FirstName` VARCHAR(255),
  `MiddleName` VARCHAR(255),
  `LastName` VARCHAR(255),
  `Suffix` VARCHAR(255),
  `CompanyName` VARCHAR(255),
  `SalesPerson` VARCHAR(255),
  `EmailAddress` VARCHAR(255),
  `Phone` VARCHAR(255),
  `PasswordHash` VARCHAR(255),
  `PasswordSalt` VARCHAR(255),
  `rowguid` VARCHAR(255),
  `ModifiedDate` DATETIME,
  PRIMARY KEY (`CustomerID`)
) CHARACTER SET latin1;

CREATE TABLE `CustomerAddress` (
  `CustomerID` INT,
  `AddressID` INT,
  `AddressType` VARCHAR(255),
  `rowguid` VARCHAR(255),
  `ModifiedDate` DATETIME,
  PRIMARY KEY (`CustomerID`, `AddressID`)
) CHARACTER SET latin1;

CREATE TABLE `Product` (
  `ProductID` INT,
  `Name` VARCHAR(255),
  `ProductNumber` VARCHAR(255),
  `Color` VARCHAR(255),
  `StandardCost` INT,
  `ListPrice` INT,
  `Size` VARCHAR(255),
  `Weight` INT,
  `ProductCategoryID` INT,
  `ProductModelID` INT,
  `SellStartDate` DATETIME,
  `SellEndDate` DATETIME,
  `DiscontinuedDate` DATETIME,
  `ThumbNailPhoto` VARCHAR(255),
  `ThumbnailPhotoFileName` VARCHAR(255),
  `rowguid` VARCHAR(255),
  `ModifiedDate` DATETIME,
  PRIMARY KEY (`ProductID`)
) CHARACTER SET latin1;

CREATE TABLE `ProductCategory` (
  `ProductCategoryID` INT,
  `ParentProductCategoryID` INT,
  `Name` VARCHAR(255),
  `rowguid` VARCHAR(255),
  `ModifiedDate` DATETIME,
  PRIMARY KEY (`ProductCategoryID`)
) CHARACTER SET latin1;

CREATE TABLE `ProductDescription` (
  `ProductDescriptionID` INT,
  `Description` VARCHAR(255),
  `rowguid` VARCHAR(255),
  `ModifiedDate` DATETIME,
  PRIMARY KEY (`ProductDescriptionID`)
) CHARACTER SET latin1;

CREATE TABLE `ProductModel` (
  `ProductModelID` INT,
  `Name` VARCHAR(255),
  `CatalogDescription` VARCHAR(255),
  `rowguid` VARCHAR(255),
  `ModifiedDate` DATETIME,
  PRIMARY KEY (`ProductModelID`)
) CHARACTER SET latin1;

CREATE TABLE `ProductModelProductDescription` (
  `ProductModelID` INT,
  `ProductDescriptionID` INT,
  `Culture` VARCHAR(255),
  `rowguid` VARCHAR(255),
  `ModifiedDate` DATETIME,
  PRIMARY KEY (`ProductModelID`, `ProductDescriptionID`, `Culture`)
) CHARACTER SET latin1;

CREATE TABLE `SalesOrderHeader` (
  `SalesOrderID` INT,
  `RevisionNumber` INT,
  `OrderDate` DATETIME,
  `DueDate` DATETIME,
  `ShipDate` DATETIME,
  `Status` INT,
  `OnlineOrderFlag` INT,
  `SalesOrderNumber` VARCHAR(255),
  `PurchaseOrderNumber` INT,
  `AccountNumber` VARCHAR(255),
  `CustomerID` INT,
  `ShipToAddressID` INT,
  `BillToAddressID` INT,
  `ShipMethod` VARCHAR(255),
  `CreditCardApprovalCode` VARCHAR(255),
  `SubTotal` INT,
  `TaxAmt` INT,
  `Freight` INT,
  `TotalDue` INT,
  `Comment` VARCHAR(255),
  `rowguid` VARCHAR(255),
  `ModifiedDate` DATETIME,
  PRIMARY KEY (`SalesOrderID`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_137919_instawltdb') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_137919_instawltdb') AS relationships;
DROP DATABASE `db_137919_instawltdb`;
UNINSTALL EXTENSION vsql_schematron;
