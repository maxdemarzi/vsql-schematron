INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 136511_22-order-of-fks-doesnt-matter
--echo #
CREATE DATABASE `db_136511_22_order_of_fks_doesnt_matter`;
USE `db_136511_22_order_of_fks_doesnt_matter`;

CREATE TABLE `aaa` (
  `id` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

CREATE TABLE `bbb` (
  `a` INT,
  `b` VARCHAR(64)
) CHARACTER SET latin1;

CREATE TABLE `ccc` (
  `a` INT,
  `b` VARCHAR(64)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_136511_22_order_of_fks_doesnt_matter') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_136511_22_order_of_fks_doesnt_matter') AS relationships;
DROP DATABASE `db_136511_22_order_of_fks_doesnt_matter`;
UNINSTALL EXTENSION vsql_schematron;
