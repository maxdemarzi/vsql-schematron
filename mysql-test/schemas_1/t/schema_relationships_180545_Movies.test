INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 180545_Movies
--echo #
CREATE DATABASE `db_180545_Movies`;
USE `db_180545_Movies`;

CREATE TABLE `ACTOR` (
  `ACT_ID` DECIMAL(10,2),
  `ACT_NAME` VARCHAR(64),
  `ACT_GENDER` VARCHAR(64),
  PRIMARY KEY (`ACT_ID`)
) CHARACTER SET latin1;

CREATE TABLE `DIRECTOR` (
  `DIR_ID` DECIMAL(10,2),
  `DIR_NAME` VARCHAR(64),
  `DIR_PHONE` DECIMAL(10,2),
  PRIMARY KEY (`DIR_ID`)
) CHARACTER SET latin1;

CREATE TABLE `MOVIES` (
  `MOV_ID` DECIMAL(10,2),
  `MOV_TITLE` VARCHAR(64),
  `MOV_YEAR` DECIMAL(10,2),
  `MOV_LANG` VARCHAR(64),
  `DIR_ID` DECIMAL(10,2),
  PRIMARY KEY (`MOV_ID`)
) CHARACTER SET latin1;

CREATE TABLE `MOVIE_CAST` (
  `ACT_ID` DECIMAL(10,2),
  `MOV_ID` DECIMAL(10,2),
  `ROLE` VARCHAR(64),
  PRIMARY KEY (`ACT_ID`, `MOV_ID`)
) CHARACTER SET latin1;

CREATE TABLE `RATING` (
  `MOV_ID` DECIMAL(10,2),
  `REV_STARS` INT
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_180545_Movies') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_180545_Movies') AS relationships;
DROP DATABASE `db_180545_Movies`;
UNINSTALL EXTENSION vsql_schematron;
