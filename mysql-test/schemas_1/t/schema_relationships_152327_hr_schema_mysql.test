INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 152327_hr-schema-mysql
--echo #
CREATE DATABASE `db_152327_hr_schema_mysql`;
USE `db_152327_hr_schema_mysql`;

CREATE TABLE `regions` (
  `region_id` INT,
  `region_name` VARCHAR(64),
  PRIMARY KEY (`region_id`)
) CHARACTER SET latin1;

CREATE TABLE `countries` (
  `country_id` VARCHAR(64),
  `country_name` VARCHAR(64),
  `region_id` INT,
  PRIMARY KEY (`country_id`)
) CHARACTER SET latin1;

CREATE TABLE `locations` (
  `location_id` INT,
  `street_address` VARCHAR(64),
  `postal_code` VARCHAR(64),
  `city` VARCHAR(64),
  `state_province` VARCHAR(64),
  `country_id` VARCHAR(64),
  PRIMARY KEY (`location_id`)
) CHARACTER SET latin1;

CREATE TABLE `departments` (
  `department_id` INT,
  `department_name` VARCHAR(64),
  `manager_id` INT,
  `location_id` INT,
  PRIMARY KEY (`department_id`)
) CHARACTER SET latin1;

CREATE TABLE `employees` (
  `employee_id` INT,
  `first_name` VARCHAR(64),
  `last_name` VARCHAR(64),
  `email` VARCHAR(64),
  `phone_number` VARCHAR(64),
  `hire_date` DATETIME,
  `salary` DECIMAL(10,2),
  `manager_id` INT,
  `department_id` INT,
  PRIMARY KEY (`employee_id`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_152327_hr_schema_mysql') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_152327_hr_schema_mysql') AS relationships;
DROP DATABASE `db_152327_hr_schema_mysql`;
UNINSTALL EXTENSION vsql_schematron;
