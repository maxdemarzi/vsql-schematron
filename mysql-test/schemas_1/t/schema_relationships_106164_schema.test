INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 106164_schema
--echo #
CREATE DATABASE `db_106164_schema`;
USE `db_106164_schema`;

CREATE TABLE `employee` (
  `id` INT,
  `first_name` VARCHAR(255),
  `last_name` VARCHAR(255),
  `roles_id` INT,
  `manager_id` INT,
  PRIMARY KEY (`id`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_106164_schema') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_106164_schema') AS relationships;
DROP DATABASE `db_106164_schema`;
UNINSTALL EXTENSION vsql_schematron;
