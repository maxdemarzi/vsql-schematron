INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 139774_membership
--echo #
CREATE DATABASE `db_139774_membership`;
USE `db_139774_membership`;

CREATE TABLE `R_AGAMA` (
  `RAGAMA_KODE` VARCHAR(255),
  `RAGAMA_NAMA` VARCHAR(255),
  PRIMARY KEY (`RAGAMA_KODE`)
) CHARACTER SET latin1;

CREATE TABLE `R_CALON` (
  `RCALON_ID` INT,
  `RSTATKAWIN_KODE` VARCHAR(255),
  `RUSERAKUN_NAMA` VARCHAR(255),
  `RJK_KODE` VARCHAR(255),
  `RKERJA_ID` INT,
  `RGOLDARAH_ID` INT,
  `RKOTA_ID` INT,
  `RWN_KODE` VARCHAR(255),
  `RAGAMA_KODE` VARCHAR(255),
  `RCALON_NAMA` VARCHAR(255),
  `RCALON_PANGGILAN` VARCHAR(255),
  `RCALON_TEMPATLAHIR` VARCHAR(255),
  `RCALON_TGLLAHIR` DATETIME,
  `RCALON_ALAMAT` VARCHAR(255),
  `RCALON_RT` VARCHAR(255),
  `RCALON_RW` VARCHAR(255),
  `RCALON_KEL` VARCHAR(255),
  `RCALON_KEC` VARCHAR(255),
  `RCALON_NOHAPE` VARCHAR(255),
  `RCALON_EMAIL` VARCHAR(255),
  `RCALON_TGLEXPIRED` DATETIME,
  `RCALON_TGLENTRI` DATETIME,
  `RCALON_TGLVERVAL` DATETIME,
  `RCALON_TGLUPDATE` DATETIME,
  PRIMARY KEY (`RCALON_ID`)
) CHARACTER SET latin1;

CREATE TABLE `R_GOLDARAH` (
  `RGOLDARAH_ID` INT,
  `RGOLDARAH_KODE` VARCHAR(255),
  PRIMARY KEY (`RGOLDARAH_ID`)
) CHARACTER SET latin1;

CREATE TABLE `R_IMAGE` (
  `RIMAGE_ID` INT,
  `RTFILE_ID` INT,
  `RIMAGE_CAPTION` VARCHAR(255),
  `RIMAGE_DESK` VARCHAR(255),
  `RIMAGE_TGLDIBUAT` DATETIME,
  `RIMAGE_TGLUPLOAD` DATETIME,
  `RIMAGE_NMFLE` VARCHAR(255),
  `RIMAGE_FOLDER` VARCHAR(255),
  PRIMARY KEY (`RIMAGE_ID`)
) CHARACTER SET latin1;

CREATE TABLE `R_JK` (
  `RJK_KODE` VARCHAR(255),
  `RJK_NAMA` VARCHAR(255),
  PRIMARY KEY (`RJK_KODE`)
) CHARACTER SET latin1;

CREATE TABLE `R_JURUSAN` (
  `RJURUSAN_KODE` VARCHAR(255),
  `RJURUSAN_NAMA` VARCHAR(255),
  PRIMARY KEY (`RJURUSAN_KODE`)
) CHARACTER SET latin1;

CREATE TABLE `R_KERJA` (
  `RKERJA_ID` INT,
  `RKERJA_NAMA` VARCHAR(255),
  PRIMARY KEY (`RKERJA_ID`)
) CHARACTER SET latin1;

CREATE TABLE `R_KOTA` (
  `RKOTA_ID` INT,
  `RKOTA_KODE` VARCHAR(255),
  `RKOTA_NAMA` VARCHAR(255),
  PRIMARY KEY (`RKOTA_ID`)
) CHARACTER SET latin1;

CREATE TABLE `R_PERSONAL` (
  `RPERSONAL_ID` INT,
  `RGOLDARAH_ID` INT,
  `RJK_KODE` VARCHAR(255),
  `RSTATKAWIN_KODE` VARCHAR(255),
  `RKERJA_ID` INT,
  `RWN_KODE` VARCHAR(255),
  `RKOTA_ID` INT,
  `RAGAMA_KODE` VARCHAR(255),
  `RPERSONAL_NAMA` VARCHAR(255),
  `RPERSONAL_PANGGILAN` VARCHAR(255),
  `RPERSONAL_TEMPATLAHR` VARCHAR(255),
  `RPERSONAL_TGLLAHIR` DATETIME,
  `RPERSONAL_ALAMAT` VARCHAR(255),
  `RPERSONAL_RT` VARCHAR(255),
  `RPERSONAL_RW` VARCHAR(255),
  `RPERSONAL_KEL` VARCHAR(255),
  `RPERSONAL_KECAMATAN` VARCHAR(255),
  `RPERSONAL_NOHAPE` VARCHAR(255),
  `RPERSONAL_EMAIL` VARCHAR(255),
  `RPERSONAL_TGLEXPIRED` DATETIME,
  `RPERSONAL_TGLENTRI` DATETIME,
  `RPERSONAL_TGLVERVAL` DATETIME,
  `RPERSONAL_TGLUPDATE` DATETIME,
  PRIMARY KEY (`RPERSONAL_ID`)
) CHARACTER SET latin1;

CREATE TABLE `R_ROLE` (
  `RROLE_KODE` VARCHAR(255),
  `RROLE_NAMA` VARCHAR(255),
  `RROLE_DESK` VARCHAR(255),
  PRIMARY KEY (`RROLE_KODE`)
) CHARACTER SET latin1;

CREATE TABLE `R_STATAKUN` (
  `RSTATAKUN_KODE` VARCHAR(255),
  `RSTATAKUN_NAMA` VARCHAR(255),
  PRIMARY KEY (`RSTATAKUN_KODE`)
) CHARACTER SET latin1;

CREATE TABLE `R_STATBERITA` (
  `RSTATBERITA_KODE` VARCHAR(255),
  `RSTATBERITA_NAMA` VARCHAR(255),
  PRIMARY KEY (`RSTATBERITA_KODE`)
) CHARACTER SET latin1;

CREATE TABLE `R_STATIMAGE` (
  `RSTATIMAGE_KODE` VARCHAR(255),
  `RSTATIMAGE_DESK` VARCHAR(255),
  PRIMARY KEY (`RSTATIMAGE_KODE`)
) CHARACTER SET latin1;

CREATE TABLE `R_STATKAWIN` (
  `RSTATKAWIN_KODE` VARCHAR(255),
  `RSTATKAWIN_NAMA` VARCHAR(255),
  PRIMARY KEY (`RSTATKAWIN_KODE`)
) CHARACTER SET latin1;

CREATE TABLE `R_TFILE` (
  `RTFILE_ID` INT,
  `RTFILE_EXT` VARCHAR(255),
  `RTFILE_DESK` VARCHAR(255),
  PRIMARY KEY (`RTFILE_ID`)
) CHARACTER SET latin1;

CREATE TABLE `R_USERAKUN` (
  `RUSERAKUN_NAMA` VARCHAR(255),
  `RSTATAKUN_KODE` VARCHAR(255),
  `RPERSONAL_ID` INT,
  `RROLE_KODE` VARCHAR(255),
  `RUSERAKUN_PASSW` VARCHAR(255),
  `RUSERAKUN_TGLCREATE` DATETIME,
  `RUSERAKUN_TGLVER` DATETIME,
  `RUSERAKUN_TGLEXPIRED` DATETIME,
  PRIMARY KEY (`RUSERAKUN_NAMA`)
) CHARACTER SET latin1;

CREATE TABLE `R_USERDATA` (
  `RUSERAKUN_NAMA` VARCHAR(255),
  `RUSERDATA_TAHUN` INT,
  `RSTATAKUN_KODE` VARCHAR(255),
  `RUSERDATA_TGLMULAI` DATETIME,
  PRIMARY KEY (`RUSERAKUN_NAMA`, `RUSERDATA_TAHUN`)
) CHARACTER SET latin1;

CREATE TABLE `R_WN` (
  `RWN_KODE` VARCHAR(255),
  `RWN_NAMA` VARCHAR(255),
  PRIMARY KEY (`RWN_KODE`)
) CHARACTER SET latin1;

CREATE TABLE `T_BERITA` (
  `TBERITA_ID` INT,
  `RSTATBERITA_KODE` VARCHAR(255),
  `RUSERAKUN_NAMA` VARCHAR(255),
  `TBERITA_TGL` DATETIME,
  `TBERITA_JUDUL` VARCHAR(255),
  `TBERITA_DESK` VARCHAR(255),
  `TBERITA_URL` VARCHAR(255),
  `TBERITA_TGLENTRI` DATETIME,
  PRIMARY KEY (`TBERITA_ID`)
) CHARACTER SET latin1;

CREATE TABLE `T_CALONIMAGE` (
  `RIMAGE_ID` INT,
  `RCALON_ID` INT,
  `TCALONIMAGE_TGLMAP` DATETIME,
  PRIMARY KEY (`RIMAGE_ID`, `RCALON_ID`)
) CHARACTER SET latin1;

CREATE TABLE `T_KELASMEMBER` (
  `RJURUSAN_KODE` VARCHAR(255),
  `RPERSONAL_ID` INT,
  `TKLSMEMBER_NO` VARCHAR(255),
  `TKLSMEMBER_THN` INT,
  PRIMARY KEY (`RJURUSAN_KODE`, `RPERSONAL_ID`)
) CHARACTER SET latin1;

CREATE TABLE `T_PERSIMAGE` (
  `RIMAGE_ID` INT,
  `RPERSONAL_ID` INT,
  `RSTATIMAGE_KODE` VARCHAR(255),
  `TPERSIMAGE_TGLMAP` DATETIME,
  PRIMARY KEY (`RIMAGE_ID`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_139774_membership') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_139774_membership') AS relationships;
DROP DATABASE `db_139774_membership`;
UNINSTALL EXTENSION vsql_schematron;
