INSTALL EXTENSION vsql_schematron;
--echo #
--echo # Test relationships for schema 113604_schema
--echo #
CREATE DATABASE `db_113604_schema`;
USE `db_113604_schema`;

CREATE TABLE `department` (
  `department_id` INT,
  `name` VARCHAR(255),
  PRIMARY KEY (`department_id`)
) CHARACTER SET latin1;

CREATE TABLE `role` (
  `role_id` INT,
  `title` VARCHAR(255),
  `salary` DECIMAL(10,2),
  `department_id` INT,
  PRIMARY KEY (`role_id`)
) CHARACTER SET latin1;

CREATE TABLE `employee` (
  `employee_id` INT,
  `first_name` VARCHAR(255),
  `last_name` VARCHAR(255),
  `role_id` INT,
  `department_id` INT,
  `manager_id` INT,
  PRIMARY KEY (`employee_id`)
) CHARACTER SET latin1;

--disable_query_log
SET GLOBAL vsql_schematron.schema_cache_enabled = ON;
--enable_query_log
--let $wait_counter= 100
while ($wait_counter)
{
  --let $ready= query_get_value(SELECT vsql_schema_cache_ready('db_113604_schema') AS r, r, 1)
  if ($ready)
  {
    --let $wait_counter= 0
  }
  if (!$ready)
  {
    sleep 0.1;
    --dec $wait_counter
  }
}
SELECT vsql_schema_relationships('db_113604_schema') AS relationships;
DROP DATABASE `db_113604_schema`;
UNINSTALL EXTENSION vsql_schematron;
