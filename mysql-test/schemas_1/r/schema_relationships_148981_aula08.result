INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 148981_aula08
#
CREATE DATABASE `db_148981_aula08`;
USE `db_148981_aula08`;
CREATE TABLE `EMPREGADO` (
`codigo` INT,
`nome` VARCHAR(64),
`cpf` VARCHAR(64),
`sexo` VARCHAR(64),
`datanasc` DATETIME,
`logradouro` VARCHAR(64),
`nro` INT,
`salario` DECIMAL(10,2),
`depto_num` INT,
`codigo_ger` INT,
PRIMARY KEY (`codigo`)
) CHARACTER SET latin1;
CREATE TABLE `DEPARTAMENTO` (
`numero` INT,
`nome` VARCHAR(64),
`codigo_ger` INT,
`data_inicio_ger` DATETIME,
PRIMARY KEY (`numero`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_148981_aula08') AS relationships;
relationships
Database: db_148981_aula08

Table Connections:
------------------
(No relationships found)

DROP DATABASE `db_148981_aula08`;
UNINSTALL EXTENSION vsql_schematron;
