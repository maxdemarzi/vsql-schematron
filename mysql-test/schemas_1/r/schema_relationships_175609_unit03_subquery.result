INSTALL EXTENSION vsql_schematron;
#
# Test relationships for schema 175609_unit03_subquery
#
CREATE DATABASE `db_175609_unit03_subquery`;
USE `db_175609_unit03_subquery`;
CREATE TABLE `JOBS` (
`JOB_ID` DECIMAL(10,2),
`JOB_NAME` VARCHAR(64),
PRIMARY KEY (`JOB_ID`)
) CHARACTER SET latin1;
CREATE TABLE `LOCATIONS` (
`LOCATION_ID` DECIMAL(10,2),
`LOCATION_NAME` VARCHAR(64),
PRIMARY KEY (`LOCATION_ID`)
) CHARACTER SET latin1;
CREATE TABLE `DEPARTMENTS` (
`DEPARTMENT_ID` DECIMAL(10,2),
`DEPARTMENT_NAME` VARCHAR(64),
`MANAGER_ID` DECIMAL(10,2),
`LOCATION_ID` DECIMAL(10,2),
PRIMARY KEY (`DEPARTMENT_ID`)
) CHARACTER SET latin1;
CREATE TABLE `MANAGERS` (
`MANAGER_ID` DECIMAL(10,2),
`DEPARTMENT_ID` DECIMAL(10,2),
`MANAGER_NAME` VARCHAR(64),
PRIMARY KEY (`MANAGER_ID`)
) CHARACTER SET latin1;
CREATE TABLE `EMPLOYEES` (
`EMPLOYEE_ID` DECIMAL(10,2),
`FIRST_NAME` VARCHAR(64),
`LAST_NAME` VARCHAR(64),
`EMAIL` VARCHAR(64),
`PHONE_NUMBER` DECIMAL(10,2),
`HIRED_DATE` DATETIME,
`JOB_ID` DECIMAL(10,2),
`SALARY` DECIMAL(10,2),
`COMMISSION_PCT` DECIMAL(10,2),
`MANAGER_ID` DECIMAL(10,2),
`DEPARTMENT_ID` DECIMAL(10,2),
PRIMARY KEY (`EMPLOYEE_ID`)
) CHARACTER SET latin1;
SELECT vsql_schema_relationships('db_175609_unit03_subquery') AS relationships;
relationships
Database: db_175609_unit03_subquery

Table Connections:
------------------
[Implied]  DEPARTMENTS.LOCATION_ID -> LOCATIONS.LOCATION_ID
[Implied]  DEPARTMENTS.MANAGER_ID -> MANAGERS.MANAGER_ID
[Implied]  EMPLOYEES.DEPARTMENT_ID -> DEPARTMENTS.DEPARTMENT_ID
[Implied]  EMPLOYEES.JOB_ID -> JOBS.JOB_ID
[Implied]  EMPLOYEES.MANAGER_ID -> MANAGERS.MANAGER_ID
[Implied]  MANAGERS.DEPARTMENT_ID -> DEPARTMENTS.DEPARTMENT_ID

DROP DATABASE `db_175609_unit03_subquery`;
UNINSTALL EXTENSION vsql_schematron;
